<?php

/**
 * Base class that represents a row from the 'j005t_transaccion' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Wed Nov 11 13:50:04 2015
 *
 * @package    lib.model.om
 */
abstract class BaseJ005tTransaccion extends BaseObject  implements Persistent {


  const PEER = 'J005tTransaccionPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        J005tTransaccionPeer
	 */
	protected static $peer;

	/**
	 * The value for the co_transaccion field.
	 * @var        string
	 */
	protected $co_transaccion;

	/**
	 * The value for the fe_transaccion field.
	 * @var        string
	 */
	protected $fe_transaccion;

	/**
	 * The value for the co_usuario field.
	 * @var        string
	 */
	protected $co_usuario;

	/**
	 * The value for the tx_ruta field.
	 * @var        string
	 */
	protected $tx_ruta;

	/**
	 * The value for the nb_archivo field.
	 * @var        string
	 */
	protected $nb_archivo;

	/**
	 * @var        J002tUsuario
	 */
	protected $aJ002tUsuario;

	/**
	 * @var        array J006tSolicitud[] Collection to store aggregation of J006tSolicitud objects.
	 */
	protected $collJ006tSolicituds;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ006tSolicituds.
	 */
	private $lastJ006tSolicitudCriteria = null;

	/**
	 * @var        array J010tEvaluacion[] Collection to store aggregation of J010tEvaluacion objects.
	 */
	protected $collJ010tEvaluacions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ010tEvaluacions.
	 */
	private $lastJ010tEvaluacionCriteria = null;

	/**
	 * @var        array J011tVulnerabilidad[] Collection to store aggregation of J011tVulnerabilidad objects.
	 */
	protected $collJ011tVulnerabilidads;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ011tVulnerabilidads.
	 */
	private $lastJ011tVulnerabilidadCriteria = null;

	/**
	 * @var        array J012tCronograma[] Collection to store aggregation of J012tCronograma objects.
	 */
	protected $collJ012tCronogramasRelatedByCoTransaccionEnlace;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ012tCronogramasRelatedByCoTransaccionEnlace.
	 */
	private $lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria = null;

	/**
	 * @var        array J012tCronograma[] Collection to store aggregation of J012tCronograma objects.
	 */
	protected $collJ012tCronogramasRelatedByCoTransaccion;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ012tCronogramasRelatedByCoTransaccion.
	 */
	private $lastJ012tCronogramaRelatedByCoTransaccionCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseJ005tTransaccion object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
	}

	/**
	 * Get the [co_transaccion] column value.
	 * 
	 * @return     string
	 */
	public function getCoTransaccion()
	{
		return $this->co_transaccion;
	}

	/**
	 * Get the [optionally formatted] temporal [fe_transaccion] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFeTransaccion($format = 'Y-m-d')
	{
		if ($this->fe_transaccion === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->fe_transaccion);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fe_transaccion, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [co_usuario] column value.
	 * 
	 * @return     string
	 */
	public function getCoUsuario()
	{
		return $this->co_usuario;
	}

	/**
	 * Get the [tx_ruta] column value.
	 * 
	 * @return     string
	 */
	public function getTxRuta()
	{
		return $this->tx_ruta;
	}

	/**
	 * Get the [nb_archivo] column value.
	 * 
	 * @return     string
	 */
	public function getNbArchivo()
	{
		return $this->nb_archivo;
	}

	/**
	 * Set the value of [co_transaccion] column.
	 * 
	 * @param      string $v new value
	 * @return     J005tTransaccion The current object (for fluent API support)
	 */
	public function setCoTransaccion($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_transaccion !== $v) {
			$this->co_transaccion = $v;
			$this->modifiedColumns[] = J005tTransaccionPeer::CO_TRANSACCION;
		}

		return $this;
	} // setCoTransaccion()

	/**
	 * Sets the value of [fe_transaccion] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     J005tTransaccion The current object (for fluent API support)
	 */
	public function setFeTransaccion($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fe_transaccion !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fe_transaccion !== null && $tmpDt = new DateTime($this->fe_transaccion)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fe_transaccion = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = J005tTransaccionPeer::FE_TRANSACCION;
			}
		} // if either are not null

		return $this;
	} // setFeTransaccion()

	/**
	 * Set the value of [co_usuario] column.
	 * 
	 * @param      string $v new value
	 * @return     J005tTransaccion The current object (for fluent API support)
	 */
	public function setCoUsuario($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_usuario !== $v) {
			$this->co_usuario = $v;
			$this->modifiedColumns[] = J005tTransaccionPeer::CO_USUARIO;
		}

		if ($this->aJ002tUsuario !== null && $this->aJ002tUsuario->getCoUsuario() !== $v) {
			$this->aJ002tUsuario = null;
		}

		return $this;
	} // setCoUsuario()

	/**
	 * Set the value of [tx_ruta] column.
	 * 
	 * @param      string $v new value
	 * @return     J005tTransaccion The current object (for fluent API support)
	 */
	public function setTxRuta($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_ruta !== $v) {
			$this->tx_ruta = $v;
			$this->modifiedColumns[] = J005tTransaccionPeer::TX_RUTA;
		}

		return $this;
	} // setTxRuta()

	/**
	 * Set the value of [nb_archivo] column.
	 * 
	 * @param      string $v new value
	 * @return     J005tTransaccion The current object (for fluent API support)
	 */
	public function setNbArchivo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nb_archivo !== $v) {
			$this->nb_archivo = $v;
			$this->modifiedColumns[] = J005tTransaccionPeer::NB_ARCHIVO;
		}

		return $this;
	} // setNbArchivo()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array())) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->co_transaccion = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->fe_transaccion = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->co_usuario = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->tx_ruta = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->nb_archivo = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 5; // 5 = J005tTransaccionPeer::NUM_COLUMNS - J005tTransaccionPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating J005tTransaccion object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aJ002tUsuario !== null && $this->co_usuario !== $this->aJ002tUsuario->getCoUsuario()) {
			$this->aJ002tUsuario = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J005tTransaccionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = J005tTransaccionPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aJ002tUsuario = null;
			$this->collJ006tSolicituds = null;
			$this->lastJ006tSolicitudCriteria = null;

			$this->collJ010tEvaluacions = null;
			$this->lastJ010tEvaluacionCriteria = null;

			$this->collJ011tVulnerabilidads = null;
			$this->lastJ011tVulnerabilidadCriteria = null;

			$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = null;
			$this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria = null;

			$this->collJ012tCronogramasRelatedByCoTransaccion = null;
			$this->lastJ012tCronogramaRelatedByCoTransaccionCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ005tTransaccion:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J005tTransaccionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			J005tTransaccionPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseJ005tTransaccion:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ005tTransaccion:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J005tTransaccionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseJ005tTransaccion:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			J005tTransaccionPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aJ002tUsuario !== null) {
				if ($this->aJ002tUsuario->isModified() || $this->aJ002tUsuario->isNew()) {
					$affectedRows += $this->aJ002tUsuario->save($con);
				}
				$this->setJ002tUsuario($this->aJ002tUsuario);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = J005tTransaccionPeer::CO_TRANSACCION;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = J005tTransaccionPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setCoTransaccion($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += J005tTransaccionPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collJ006tSolicituds !== null) {
				foreach ($this->collJ006tSolicituds as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ010tEvaluacions !== null) {
				foreach ($this->collJ010tEvaluacions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ011tVulnerabilidads !== null) {
				foreach ($this->collJ011tVulnerabilidads as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace !== null) {
				foreach ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ012tCronogramasRelatedByCoTransaccion !== null) {
				foreach ($this->collJ012tCronogramasRelatedByCoTransaccion as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aJ002tUsuario !== null) {
				if (!$this->aJ002tUsuario->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJ002tUsuario->getValidationFailures());
				}
			}


			if (($retval = J005tTransaccionPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collJ006tSolicituds !== null) {
					foreach ($this->collJ006tSolicituds as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ010tEvaluacions !== null) {
					foreach ($this->collJ010tEvaluacions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ011tVulnerabilidads !== null) {
					foreach ($this->collJ011tVulnerabilidads as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace !== null) {
					foreach ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ012tCronogramasRelatedByCoTransaccion !== null) {
					foreach ($this->collJ012tCronogramasRelatedByCoTransaccion as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = J005tTransaccionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCoTransaccion();
				break;
			case 1:
				return $this->getFeTransaccion();
				break;
			case 2:
				return $this->getCoUsuario();
				break;
			case 3:
				return $this->getTxRuta();
				break;
			case 4:
				return $this->getNbArchivo();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = J005tTransaccionPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCoTransaccion(),
			$keys[1] => $this->getFeTransaccion(),
			$keys[2] => $this->getCoUsuario(),
			$keys[3] => $this->getTxRuta(),
			$keys[4] => $this->getNbArchivo(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = J005tTransaccionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCoTransaccion($value);
				break;
			case 1:
				$this->setFeTransaccion($value);
				break;
			case 2:
				$this->setCoUsuario($value);
				break;
			case 3:
				$this->setTxRuta($value);
				break;
			case 4:
				$this->setNbArchivo($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = J005tTransaccionPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCoTransaccion($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setFeTransaccion($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCoUsuario($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setTxRuta($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setNbArchivo($arr[$keys[4]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);

		if ($this->isColumnModified(J005tTransaccionPeer::CO_TRANSACCION)) $criteria->add(J005tTransaccionPeer::CO_TRANSACCION, $this->co_transaccion);
		if ($this->isColumnModified(J005tTransaccionPeer::FE_TRANSACCION)) $criteria->add(J005tTransaccionPeer::FE_TRANSACCION, $this->fe_transaccion);
		if ($this->isColumnModified(J005tTransaccionPeer::CO_USUARIO)) $criteria->add(J005tTransaccionPeer::CO_USUARIO, $this->co_usuario);
		if ($this->isColumnModified(J005tTransaccionPeer::TX_RUTA)) $criteria->add(J005tTransaccionPeer::TX_RUTA, $this->tx_ruta);
		if ($this->isColumnModified(J005tTransaccionPeer::NB_ARCHIVO)) $criteria->add(J005tTransaccionPeer::NB_ARCHIVO, $this->nb_archivo);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);

		$criteria->add(J005tTransaccionPeer::CO_TRANSACCION, $this->co_transaccion);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getCoTransaccion();
	}

	/**
	 * Generic method to set the primary key (co_transaccion column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setCoTransaccion($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of J005tTransaccion (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setFeTransaccion($this->fe_transaccion);

		$copyObj->setCoUsuario($this->co_usuario);

		$copyObj->setTxRuta($this->tx_ruta);

		$copyObj->setNbArchivo($this->nb_archivo);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getJ006tSolicituds() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ006tSolicitud($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ010tEvaluacions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ010tEvaluacion($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ011tVulnerabilidads() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ011tVulnerabilidad($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ012tCronogramasRelatedByCoTransaccionEnlace() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ012tCronogramaRelatedByCoTransaccionEnlace($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ012tCronogramasRelatedByCoTransaccion() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ012tCronogramaRelatedByCoTransaccion($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setCoTransaccion(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     J005tTransaccion Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     J005tTransaccionPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new J005tTransaccionPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a J002tUsuario object.
	 *
	 * @param      J002tUsuario $v
	 * @return     J005tTransaccion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJ002tUsuario(J002tUsuario $v = null)
	{
		if ($v === null) {
			$this->setCoUsuario(NULL);
		} else {
			$this->setCoUsuario($v->getCoUsuario());
		}

		$this->aJ002tUsuario = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the J002tUsuario object, it will not be re-added.
		if ($v !== null) {
			$v->addJ005tTransaccion($this);
		}

		return $this;
	}


	/**
	 * Get the associated J002tUsuario object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     J002tUsuario The associated J002tUsuario object.
	 * @throws     PropelException
	 */
	public function getJ002tUsuario(PropelPDO $con = null)
	{
		if ($this->aJ002tUsuario === null && (($this->co_usuario !== "" && $this->co_usuario !== null))) {
			$c = new Criteria(J002tUsuarioPeer::DATABASE_NAME);
			$c->add(J002tUsuarioPeer::CO_USUARIO, $this->co_usuario);
			$this->aJ002tUsuario = J002tUsuarioPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJ002tUsuario->addJ005tTransaccions($this);
			 */
		}
		return $this->aJ002tUsuario;
	}

	/**
	 * Clears out the collJ006tSolicituds collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ006tSolicituds()
	 */
	public function clearJ006tSolicituds()
	{
		$this->collJ006tSolicituds = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ006tSolicituds collection (array).
	 *
	 * By default this just sets the collJ006tSolicituds collection to an empty array (like clearcollJ006tSolicituds());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ006tSolicituds()
	{
		$this->collJ006tSolicituds = array();
	}

	/**
	 * Gets an array of J006tSolicitud objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion has previously been saved, it will retrieve
	 * related J006tSolicituds from storage. If this J005tTransaccion is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J006tSolicitud[]
	 * @throws     PropelException
	 */
	public function getJ006tSolicituds($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ006tSolicituds === null) {
			if ($this->isNew()) {
			   $this->collJ006tSolicituds = array();
			} else {

				$criteria->add(J006tSolicitudPeer::CO_TRANSACCION, $this->co_transaccion);

				J006tSolicitudPeer::addSelectColumns($criteria);
				$this->collJ006tSolicituds = J006tSolicitudPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J006tSolicitudPeer::CO_TRANSACCION, $this->co_transaccion);

				J006tSolicitudPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ006tSolicitudCriteria) || !$this->lastJ006tSolicitudCriteria->equals($criteria)) {
					$this->collJ006tSolicituds = J006tSolicitudPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ006tSolicitudCriteria = $criteria;
		return $this->collJ006tSolicituds;
	}

	/**
	 * Returns the number of related J006tSolicitud objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J006tSolicitud objects.
	 * @throws     PropelException
	 */
	public function countJ006tSolicituds(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ006tSolicituds === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J006tSolicitudPeer::CO_TRANSACCION, $this->co_transaccion);

				$count = J006tSolicitudPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J006tSolicitudPeer::CO_TRANSACCION, $this->co_transaccion);

				if (!isset($this->lastJ006tSolicitudCriteria) || !$this->lastJ006tSolicitudCriteria->equals($criteria)) {
					$count = J006tSolicitudPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ006tSolicituds);
				}
			} else {
				$count = count($this->collJ006tSolicituds);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J006tSolicitud object to this object
	 * through the J006tSolicitud foreign key attribute.
	 *
	 * @param      J006tSolicitud $l J006tSolicitud
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ006tSolicitud(J006tSolicitud $l)
	{
		if ($this->collJ006tSolicituds === null) {
			$this->initJ006tSolicituds();
		}
		if (!in_array($l, $this->collJ006tSolicituds, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ006tSolicituds, $l);
			$l->setJ005tTransaccion($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion is new, it will return
	 * an empty collection; or if this J005tTransaccion has previously
	 * been saved, it will retrieve related J006tSolicituds from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J005tTransaccion.
	 */
	public function getJ006tSolicitudsJoinJ001tMenu($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ006tSolicituds === null) {
			if ($this->isNew()) {
				$this->collJ006tSolicituds = array();
			} else {

				$criteria->add(J006tSolicitudPeer::CO_TRANSACCION, $this->co_transaccion);

				$this->collJ006tSolicituds = J006tSolicitudPeer::doSelectJoinJ001tMenu($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J006tSolicitudPeer::CO_TRANSACCION, $this->co_transaccion);

			if (!isset($this->lastJ006tSolicitudCriteria) || !$this->lastJ006tSolicitudCriteria->equals($criteria)) {
				$this->collJ006tSolicituds = J006tSolicitudPeer::doSelectJoinJ001tMenu($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ006tSolicitudCriteria = $criteria;

		return $this->collJ006tSolicituds;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion is new, it will return
	 * an empty collection; or if this J005tTransaccion has previously
	 * been saved, it will retrieve related J006tSolicituds from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J005tTransaccion.
	 */
	public function getJ006tSolicitudsJoinJ007tRegion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ006tSolicituds === null) {
			if ($this->isNew()) {
				$this->collJ006tSolicituds = array();
			} else {

				$criteria->add(J006tSolicitudPeer::CO_TRANSACCION, $this->co_transaccion);

				$this->collJ006tSolicituds = J006tSolicitudPeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J006tSolicitudPeer::CO_TRANSACCION, $this->co_transaccion);

			if (!isset($this->lastJ006tSolicitudCriteria) || !$this->lastJ006tSolicitudCriteria->equals($criteria)) {
				$this->collJ006tSolicituds = J006tSolicitudPeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ006tSolicitudCriteria = $criteria;

		return $this->collJ006tSolicituds;
	}

	/**
	 * Clears out the collJ010tEvaluacions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ010tEvaluacions()
	 */
	public function clearJ010tEvaluacions()
	{
		$this->collJ010tEvaluacions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ010tEvaluacions collection (array).
	 *
	 * By default this just sets the collJ010tEvaluacions collection to an empty array (like clearcollJ010tEvaluacions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ010tEvaluacions()
	{
		$this->collJ010tEvaluacions = array();
	}

	/**
	 * Gets an array of J010tEvaluacion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion has previously been saved, it will retrieve
	 * related J010tEvaluacions from storage. If this J005tTransaccion is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J010tEvaluacion[]
	 * @throws     PropelException
	 */
	public function getJ010tEvaluacions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ010tEvaluacions === null) {
			if ($this->isNew()) {
			   $this->collJ010tEvaluacions = array();
			} else {

				$criteria->add(J010tEvaluacionPeer::CO_TRANSACCION, $this->co_transaccion);

				J010tEvaluacionPeer::addSelectColumns($criteria);
				$this->collJ010tEvaluacions = J010tEvaluacionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J010tEvaluacionPeer::CO_TRANSACCION, $this->co_transaccion);

				J010tEvaluacionPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ010tEvaluacionCriteria) || !$this->lastJ010tEvaluacionCriteria->equals($criteria)) {
					$this->collJ010tEvaluacions = J010tEvaluacionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ010tEvaluacionCriteria = $criteria;
		return $this->collJ010tEvaluacions;
	}

	/**
	 * Returns the number of related J010tEvaluacion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J010tEvaluacion objects.
	 * @throws     PropelException
	 */
	public function countJ010tEvaluacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ010tEvaluacions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J010tEvaluacionPeer::CO_TRANSACCION, $this->co_transaccion);

				$count = J010tEvaluacionPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J010tEvaluacionPeer::CO_TRANSACCION, $this->co_transaccion);

				if (!isset($this->lastJ010tEvaluacionCriteria) || !$this->lastJ010tEvaluacionCriteria->equals($criteria)) {
					$count = J010tEvaluacionPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ010tEvaluacions);
				}
			} else {
				$count = count($this->collJ010tEvaluacions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J010tEvaluacion object to this object
	 * through the J010tEvaluacion foreign key attribute.
	 *
	 * @param      J010tEvaluacion $l J010tEvaluacion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ010tEvaluacion(J010tEvaluacion $l)
	{
		if ($this->collJ010tEvaluacions === null) {
			$this->initJ010tEvaluacions();
		}
		if (!in_array($l, $this->collJ010tEvaluacions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ010tEvaluacions, $l);
			$l->setJ005tTransaccion($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion is new, it will return
	 * an empty collection; or if this J005tTransaccion has previously
	 * been saved, it will retrieve related J010tEvaluacions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J005tTransaccion.
	 */
	public function getJ010tEvaluacionsJoinJ002tUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ010tEvaluacions === null) {
			if ($this->isNew()) {
				$this->collJ010tEvaluacions = array();
			} else {

				$criteria->add(J010tEvaluacionPeer::CO_TRANSACCION, $this->co_transaccion);

				$this->collJ010tEvaluacions = J010tEvaluacionPeer::doSelectJoinJ002tUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J010tEvaluacionPeer::CO_TRANSACCION, $this->co_transaccion);

			if (!isset($this->lastJ010tEvaluacionCriteria) || !$this->lastJ010tEvaluacionCriteria->equals($criteria)) {
				$this->collJ010tEvaluacions = J010tEvaluacionPeer::doSelectJoinJ002tUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ010tEvaluacionCriteria = $criteria;

		return $this->collJ010tEvaluacions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion is new, it will return
	 * an empty collection; or if this J005tTransaccion has previously
	 * been saved, it will retrieve related J010tEvaluacions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J005tTransaccion.
	 */
	public function getJ010tEvaluacionsJoinJ007tRegion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ010tEvaluacions === null) {
			if ($this->isNew()) {
				$this->collJ010tEvaluacions = array();
			} else {

				$criteria->add(J010tEvaluacionPeer::CO_TRANSACCION, $this->co_transaccion);

				$this->collJ010tEvaluacions = J010tEvaluacionPeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J010tEvaluacionPeer::CO_TRANSACCION, $this->co_transaccion);

			if (!isset($this->lastJ010tEvaluacionCriteria) || !$this->lastJ010tEvaluacionCriteria->equals($criteria)) {
				$this->collJ010tEvaluacions = J010tEvaluacionPeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ010tEvaluacionCriteria = $criteria;

		return $this->collJ010tEvaluacions;
	}

	/**
	 * Clears out the collJ011tVulnerabilidads collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ011tVulnerabilidads()
	 */
	public function clearJ011tVulnerabilidads()
	{
		$this->collJ011tVulnerabilidads = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ011tVulnerabilidads collection (array).
	 *
	 * By default this just sets the collJ011tVulnerabilidads collection to an empty array (like clearcollJ011tVulnerabilidads());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ011tVulnerabilidads()
	{
		$this->collJ011tVulnerabilidads = array();
	}

	/**
	 * Gets an array of J011tVulnerabilidad objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion has previously been saved, it will retrieve
	 * related J011tVulnerabilidads from storage. If this J005tTransaccion is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J011tVulnerabilidad[]
	 * @throws     PropelException
	 */
	public function getJ011tVulnerabilidads($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ011tVulnerabilidads === null) {
			if ($this->isNew()) {
			   $this->collJ011tVulnerabilidads = array();
			} else {

				$criteria->add(J011tVulnerabilidadPeer::CO_TRANSACCION, $this->co_transaccion);

				J011tVulnerabilidadPeer::addSelectColumns($criteria);
				$this->collJ011tVulnerabilidads = J011tVulnerabilidadPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J011tVulnerabilidadPeer::CO_TRANSACCION, $this->co_transaccion);

				J011tVulnerabilidadPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ011tVulnerabilidadCriteria) || !$this->lastJ011tVulnerabilidadCriteria->equals($criteria)) {
					$this->collJ011tVulnerabilidads = J011tVulnerabilidadPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ011tVulnerabilidadCriteria = $criteria;
		return $this->collJ011tVulnerabilidads;
	}

	/**
	 * Returns the number of related J011tVulnerabilidad objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J011tVulnerabilidad objects.
	 * @throws     PropelException
	 */
	public function countJ011tVulnerabilidads(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ011tVulnerabilidads === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J011tVulnerabilidadPeer::CO_TRANSACCION, $this->co_transaccion);

				$count = J011tVulnerabilidadPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J011tVulnerabilidadPeer::CO_TRANSACCION, $this->co_transaccion);

				if (!isset($this->lastJ011tVulnerabilidadCriteria) || !$this->lastJ011tVulnerabilidadCriteria->equals($criteria)) {
					$count = J011tVulnerabilidadPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ011tVulnerabilidads);
				}
			} else {
				$count = count($this->collJ011tVulnerabilidads);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J011tVulnerabilidad object to this object
	 * through the J011tVulnerabilidad foreign key attribute.
	 *
	 * @param      J011tVulnerabilidad $l J011tVulnerabilidad
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ011tVulnerabilidad(J011tVulnerabilidad $l)
	{
		if ($this->collJ011tVulnerabilidads === null) {
			$this->initJ011tVulnerabilidads();
		}
		if (!in_array($l, $this->collJ011tVulnerabilidads, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ011tVulnerabilidads, $l);
			$l->setJ005tTransaccion($this);
		}
	}

	/**
	 * Clears out the collJ012tCronogramasRelatedByCoTransaccionEnlace collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ012tCronogramasRelatedByCoTransaccionEnlace()
	 */
	public function clearJ012tCronogramasRelatedByCoTransaccionEnlace()
	{
		$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ012tCronogramasRelatedByCoTransaccionEnlace collection (array).
	 *
	 * By default this just sets the collJ012tCronogramasRelatedByCoTransaccionEnlace collection to an empty array (like clearcollJ012tCronogramasRelatedByCoTransaccionEnlace());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ012tCronogramasRelatedByCoTransaccionEnlace()
	{
		$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = array();
	}

	/**
	 * Gets an array of J012tCronograma objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion has previously been saved, it will retrieve
	 * related J012tCronogramasRelatedByCoTransaccionEnlace from storage. If this J005tTransaccion is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J012tCronograma[]
	 * @throws     PropelException
	 */
	public function getJ012tCronogramasRelatedByCoTransaccionEnlace($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace === null) {
			if ($this->isNew()) {
			   $this->collJ012tCronogramasRelatedByCoTransaccionEnlace = array();
			} else {

				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION_ENLACE, $this->co_transaccion);

				J012tCronogramaPeer::addSelectColumns($criteria);
				$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = J012tCronogramaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION_ENLACE, $this->co_transaccion);

				J012tCronogramaPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria) || !$this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria->equals($criteria)) {
					$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = J012tCronogramaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria = $criteria;
		return $this->collJ012tCronogramasRelatedByCoTransaccionEnlace;
	}

	/**
	 * Returns the number of related J012tCronograma objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J012tCronograma objects.
	 * @throws     PropelException
	 */
	public function countJ012tCronogramasRelatedByCoTransaccionEnlace(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION_ENLACE, $this->co_transaccion);

				$count = J012tCronogramaPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION_ENLACE, $this->co_transaccion);

				if (!isset($this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria) || !$this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria->equals($criteria)) {
					$count = J012tCronogramaPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ012tCronogramasRelatedByCoTransaccionEnlace);
				}
			} else {
				$count = count($this->collJ012tCronogramasRelatedByCoTransaccionEnlace);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J012tCronograma object to this object
	 * through the J012tCronograma foreign key attribute.
	 *
	 * @param      J012tCronograma $l J012tCronograma
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ012tCronogramaRelatedByCoTransaccionEnlace(J012tCronograma $l)
	{
		if ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace === null) {
			$this->initJ012tCronogramasRelatedByCoTransaccionEnlace();
		}
		if (!in_array($l, $this->collJ012tCronogramasRelatedByCoTransaccionEnlace, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ012tCronogramasRelatedByCoTransaccionEnlace, $l);
			$l->setJ005tTransaccionRelatedByCoTransaccionEnlace($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion is new, it will return
	 * an empty collection; or if this J005tTransaccion has previously
	 * been saved, it will retrieve related J012tCronogramasRelatedByCoTransaccionEnlace from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J005tTransaccion.
	 */
	public function getJ012tCronogramasRelatedByCoTransaccionEnlaceJoinJ002tUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace === null) {
			if ($this->isNew()) {
				$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = array();
			} else {

				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION_ENLACE, $this->co_transaccion);

				$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = J012tCronogramaPeer::doSelectJoinJ002tUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J012tCronogramaPeer::CO_TRANSACCION_ENLACE, $this->co_transaccion);

			if (!isset($this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria) || !$this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria->equals($criteria)) {
				$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = J012tCronogramaPeer::doSelectJoinJ002tUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ012tCronogramaRelatedByCoTransaccionEnlaceCriteria = $criteria;

		return $this->collJ012tCronogramasRelatedByCoTransaccionEnlace;
	}

	/**
	 * Clears out the collJ012tCronogramasRelatedByCoTransaccion collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ012tCronogramasRelatedByCoTransaccion()
	 */
	public function clearJ012tCronogramasRelatedByCoTransaccion()
	{
		$this->collJ012tCronogramasRelatedByCoTransaccion = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ012tCronogramasRelatedByCoTransaccion collection (array).
	 *
	 * By default this just sets the collJ012tCronogramasRelatedByCoTransaccion collection to an empty array (like clearcollJ012tCronogramasRelatedByCoTransaccion());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ012tCronogramasRelatedByCoTransaccion()
	{
		$this->collJ012tCronogramasRelatedByCoTransaccion = array();
	}

	/**
	 * Gets an array of J012tCronograma objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion has previously been saved, it will retrieve
	 * related J012tCronogramasRelatedByCoTransaccion from storage. If this J005tTransaccion is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J012tCronograma[]
	 * @throws     PropelException
	 */
	public function getJ012tCronogramasRelatedByCoTransaccion($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ012tCronogramasRelatedByCoTransaccion === null) {
			if ($this->isNew()) {
			   $this->collJ012tCronogramasRelatedByCoTransaccion = array();
			} else {

				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION, $this->co_transaccion);

				J012tCronogramaPeer::addSelectColumns($criteria);
				$this->collJ012tCronogramasRelatedByCoTransaccion = J012tCronogramaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION, $this->co_transaccion);

				J012tCronogramaPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ012tCronogramaRelatedByCoTransaccionCriteria) || !$this->lastJ012tCronogramaRelatedByCoTransaccionCriteria->equals($criteria)) {
					$this->collJ012tCronogramasRelatedByCoTransaccion = J012tCronogramaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ012tCronogramaRelatedByCoTransaccionCriteria = $criteria;
		return $this->collJ012tCronogramasRelatedByCoTransaccion;
	}

	/**
	 * Returns the number of related J012tCronograma objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J012tCronograma objects.
	 * @throws     PropelException
	 */
	public function countJ012tCronogramasRelatedByCoTransaccion(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ012tCronogramasRelatedByCoTransaccion === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION, $this->co_transaccion);

				$count = J012tCronogramaPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION, $this->co_transaccion);

				if (!isset($this->lastJ012tCronogramaRelatedByCoTransaccionCriteria) || !$this->lastJ012tCronogramaRelatedByCoTransaccionCriteria->equals($criteria)) {
					$count = J012tCronogramaPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ012tCronogramasRelatedByCoTransaccion);
				}
			} else {
				$count = count($this->collJ012tCronogramasRelatedByCoTransaccion);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J012tCronograma object to this object
	 * through the J012tCronograma foreign key attribute.
	 *
	 * @param      J012tCronograma $l J012tCronograma
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ012tCronogramaRelatedByCoTransaccion(J012tCronograma $l)
	{
		if ($this->collJ012tCronogramasRelatedByCoTransaccion === null) {
			$this->initJ012tCronogramasRelatedByCoTransaccion();
		}
		if (!in_array($l, $this->collJ012tCronogramasRelatedByCoTransaccion, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ012tCronogramasRelatedByCoTransaccion, $l);
			$l->setJ005tTransaccionRelatedByCoTransaccion($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J005tTransaccion is new, it will return
	 * an empty collection; or if this J005tTransaccion has previously
	 * been saved, it will retrieve related J012tCronogramasRelatedByCoTransaccion from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J005tTransaccion.
	 */
	public function getJ012tCronogramasRelatedByCoTransaccionJoinJ002tUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J005tTransaccionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ012tCronogramasRelatedByCoTransaccion === null) {
			if ($this->isNew()) {
				$this->collJ012tCronogramasRelatedByCoTransaccion = array();
			} else {

				$criteria->add(J012tCronogramaPeer::CO_TRANSACCION, $this->co_transaccion);

				$this->collJ012tCronogramasRelatedByCoTransaccion = J012tCronogramaPeer::doSelectJoinJ002tUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J012tCronogramaPeer::CO_TRANSACCION, $this->co_transaccion);

			if (!isset($this->lastJ012tCronogramaRelatedByCoTransaccionCriteria) || !$this->lastJ012tCronogramaRelatedByCoTransaccionCriteria->equals($criteria)) {
				$this->collJ012tCronogramasRelatedByCoTransaccion = J012tCronogramaPeer::doSelectJoinJ002tUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ012tCronogramaRelatedByCoTransaccionCriteria = $criteria;

		return $this->collJ012tCronogramasRelatedByCoTransaccion;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collJ006tSolicituds) {
				foreach ((array) $this->collJ006tSolicituds as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ010tEvaluacions) {
				foreach ((array) $this->collJ010tEvaluacions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ011tVulnerabilidads) {
				foreach ((array) $this->collJ011tVulnerabilidads as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ012tCronogramasRelatedByCoTransaccionEnlace) {
				foreach ((array) $this->collJ012tCronogramasRelatedByCoTransaccionEnlace as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ012tCronogramasRelatedByCoTransaccion) {
				foreach ((array) $this->collJ012tCronogramasRelatedByCoTransaccion as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collJ006tSolicituds = null;
		$this->collJ010tEvaluacions = null;
		$this->collJ011tVulnerabilidads = null;
		$this->collJ012tCronogramasRelatedByCoTransaccionEnlace = null;
		$this->collJ012tCronogramasRelatedByCoTransaccion = null;
			$this->aJ002tUsuario = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseJ005tTransaccion:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseJ005tTransaccion::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseJ005tTransaccion
