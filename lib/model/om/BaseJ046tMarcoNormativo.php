<?php

/**
 * Base class that represents a row from the 'j046t_marco_normativo' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Tue Nov 10 11:06:19 2015
 *
 * @package    lib.model.om
 */
abstract class BaseJ046tMarcoNormativo extends BaseObject  implements Persistent {


  const PEER = 'J046tMarcoNormativoPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        J046tMarcoNormativoPeer
	 */
	protected static $peer;

	/**
	 * The value for the co_marco_normativo field.
	 * @var        string
	 */
	protected $co_marco_normativo;

	/**
	 * The value for the co_documento field.
	 * @var        string
	 */
	protected $co_documento;

	/**
	 * The value for the tx_codigo_marco field.
	 * @var        string
	 */
	protected $tx_codigo_marco;

	/**
	 * The value for the tx_descripcion field.
	 * @var        string
	 */
	protected $tx_descripcion;

	/**
	 * The value for the co_created_at field.
	 * @var        string
	 */
	protected $co_created_at;

	/**
	 * The value for the co_updated_at field.
	 * @var        string
	 */
	protected $co_updated_at;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the update_at field.
	 * @var        string
	 */
	protected $update_at;

	/**
	 * @var        array J045tReferenciaMarco[] Collection to store aggregation of J045tReferenciaMarco objects.
	 */
	protected $collJ045tReferenciaMarcos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ045tReferenciaMarcos.
	 */
	private $lastJ045tReferenciaMarcoCriteria = null;

	/**
	 * @var        array J050tInformeMarco[] Collection to store aggregation of J050tInformeMarco objects.
	 */
	protected $collJ050tInformeMarcos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ050tInformeMarcos.
	 */
	private $lastJ050tInformeMarcoCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseJ046tMarcoNormativo object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
	}

	/**
	 * Get the [co_marco_normativo] column value.
	 * 
	 * @return     string
	 */
	public function getCoMarcoNormativo()
	{
		return $this->co_marco_normativo;
	}

	/**
	 * Get the [co_documento] column value.
	 * 
	 * @return     string
	 */
	public function getCoDocumento()
	{
		return $this->co_documento;
	}

	/**
	 * Get the [tx_codigo_marco] column value.
	 * 
	 * @return     string
	 */
	public function getTxCodigoMarco()
	{
		return $this->tx_codigo_marco;
	}

	/**
	 * Get the [tx_descripcion] column value.
	 * 
	 * @return     string
	 */
	public function getTxDescripcion()
	{
		return $this->tx_descripcion;
	}

	/**
	 * Get the [co_created_at] column value.
	 * 
	 * @return     string
	 */
	public function getCoCreatedAt()
	{
		return $this->co_created_at;
	}

	/**
	 * Get the [co_updated_at] column value.
	 * 
	 * @return     string
	 */
	public function getCoUpdatedAt()
	{
		return $this->co_updated_at;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d')
	{
		if ($this->created_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->created_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [update_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdateAt($format = 'Y-m-d H:i:s')
	{
		if ($this->update_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->update_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->update_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [co_marco_normativo] column.
	 * 
	 * @param      string $v new value
	 * @return     J046tMarcoNormativo The current object (for fluent API support)
	 */
	public function setCoMarcoNormativo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_marco_normativo !== $v) {
			$this->co_marco_normativo = $v;
			$this->modifiedColumns[] = J046tMarcoNormativoPeer::CO_MARCO_NORMATIVO;
		}

		return $this;
	} // setCoMarcoNormativo()

	/**
	 * Set the value of [co_documento] column.
	 * 
	 * @param      string $v new value
	 * @return     J046tMarcoNormativo The current object (for fluent API support)
	 */
	public function setCoDocumento($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_documento !== $v) {
			$this->co_documento = $v;
			$this->modifiedColumns[] = J046tMarcoNormativoPeer::CO_DOCUMENTO;
		}

		return $this;
	} // setCoDocumento()

	/**
	 * Set the value of [tx_codigo_marco] column.
	 * 
	 * @param      string $v new value
	 * @return     J046tMarcoNormativo The current object (for fluent API support)
	 */
	public function setTxCodigoMarco($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_codigo_marco !== $v) {
			$this->tx_codigo_marco = $v;
			$this->modifiedColumns[] = J046tMarcoNormativoPeer::TX_CODIGO_MARCO;
		}

		return $this;
	} // setTxCodigoMarco()

	/**
	 * Set the value of [tx_descripcion] column.
	 * 
	 * @param      string $v new value
	 * @return     J046tMarcoNormativo The current object (for fluent API support)
	 */
	public function setTxDescripcion($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_descripcion !== $v) {
			$this->tx_descripcion = $v;
			$this->modifiedColumns[] = J046tMarcoNormativoPeer::TX_DESCRIPCION;
		}

		return $this;
	} // setTxDescripcion()

	/**
	 * Set the value of [co_created_at] column.
	 * 
	 * @param      string $v new value
	 * @return     J046tMarcoNormativo The current object (for fluent API support)
	 */
	public function setCoCreatedAt($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_created_at !== $v) {
			$this->co_created_at = $v;
			$this->modifiedColumns[] = J046tMarcoNormativoPeer::CO_CREATED_AT;
		}

		return $this;
	} // setCoCreatedAt()

	/**
	 * Set the value of [co_updated_at] column.
	 * 
	 * @param      string $v new value
	 * @return     J046tMarcoNormativo The current object (for fluent API support)
	 */
	public function setCoUpdatedAt($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_updated_at !== $v) {
			$this->co_updated_at = $v;
			$this->modifiedColumns[] = J046tMarcoNormativoPeer::CO_UPDATED_AT;
		}

		return $this;
	} // setCoUpdatedAt()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     J046tMarcoNormativo The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = J046tMarcoNormativoPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [update_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     J046tMarcoNormativo The current object (for fluent API support)
	 */
	public function setUpdateAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->update_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->update_at !== null && $tmpDt = new DateTime($this->update_at)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->update_at = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = J046tMarcoNormativoPeer::UPDATE_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdateAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array())) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->co_marco_normativo = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->co_documento = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->tx_codigo_marco = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->tx_descripcion = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->co_created_at = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->co_updated_at = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->created_at = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->update_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 8; // 8 = J046tMarcoNormativoPeer::NUM_COLUMNS - J046tMarcoNormativoPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating J046tMarcoNormativo object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J046tMarcoNormativoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = J046tMarcoNormativoPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collJ045tReferenciaMarcos = null;
			$this->lastJ045tReferenciaMarcoCriteria = null;

			$this->collJ050tInformeMarcos = null;
			$this->lastJ050tInformeMarcoCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ046tMarcoNormativo:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J046tMarcoNormativoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			J046tMarcoNormativoPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseJ046tMarcoNormativo:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ046tMarcoNormativo:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(J046tMarcoNormativoPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J046tMarcoNormativoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseJ046tMarcoNormativo:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			J046tMarcoNormativoPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = J046tMarcoNormativoPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += J046tMarcoNormativoPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collJ045tReferenciaMarcos !== null) {
				foreach ($this->collJ045tReferenciaMarcos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ050tInformeMarcos !== null) {
				foreach ($this->collJ050tInformeMarcos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = J046tMarcoNormativoPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collJ045tReferenciaMarcos !== null) {
					foreach ($this->collJ045tReferenciaMarcos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ050tInformeMarcos !== null) {
					foreach ($this->collJ050tInformeMarcos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = J046tMarcoNormativoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCoMarcoNormativo();
				break;
			case 1:
				return $this->getCoDocumento();
				break;
			case 2:
				return $this->getTxCodigoMarco();
				break;
			case 3:
				return $this->getTxDescripcion();
				break;
			case 4:
				return $this->getCoCreatedAt();
				break;
			case 5:
				return $this->getCoUpdatedAt();
				break;
			case 6:
				return $this->getCreatedAt();
				break;
			case 7:
				return $this->getUpdateAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = J046tMarcoNormativoPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCoMarcoNormativo(),
			$keys[1] => $this->getCoDocumento(),
			$keys[2] => $this->getTxCodigoMarco(),
			$keys[3] => $this->getTxDescripcion(),
			$keys[4] => $this->getCoCreatedAt(),
			$keys[5] => $this->getCoUpdatedAt(),
			$keys[6] => $this->getCreatedAt(),
			$keys[7] => $this->getUpdateAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = J046tMarcoNormativoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCoMarcoNormativo($value);
				break;
			case 1:
				$this->setCoDocumento($value);
				break;
			case 2:
				$this->setTxCodigoMarco($value);
				break;
			case 3:
				$this->setTxDescripcion($value);
				break;
			case 4:
				$this->setCoCreatedAt($value);
				break;
			case 5:
				$this->setCoUpdatedAt($value);
				break;
			case 6:
				$this->setCreatedAt($value);
				break;
			case 7:
				$this->setUpdateAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = J046tMarcoNormativoPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCoMarcoNormativo($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setCoDocumento($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setTxCodigoMarco($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setTxDescripcion($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCoCreatedAt($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCoUpdatedAt($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCreatedAt($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setUpdateAt($arr[$keys[7]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);

		if ($this->isColumnModified(J046tMarcoNormativoPeer::CO_MARCO_NORMATIVO)) $criteria->add(J046tMarcoNormativoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);
		if ($this->isColumnModified(J046tMarcoNormativoPeer::CO_DOCUMENTO)) $criteria->add(J046tMarcoNormativoPeer::CO_DOCUMENTO, $this->co_documento);
		if ($this->isColumnModified(J046tMarcoNormativoPeer::TX_CODIGO_MARCO)) $criteria->add(J046tMarcoNormativoPeer::TX_CODIGO_MARCO, $this->tx_codigo_marco);
		if ($this->isColumnModified(J046tMarcoNormativoPeer::TX_DESCRIPCION)) $criteria->add(J046tMarcoNormativoPeer::TX_DESCRIPCION, $this->tx_descripcion);
		if ($this->isColumnModified(J046tMarcoNormativoPeer::CO_CREATED_AT)) $criteria->add(J046tMarcoNormativoPeer::CO_CREATED_AT, $this->co_created_at);
		if ($this->isColumnModified(J046tMarcoNormativoPeer::CO_UPDATED_AT)) $criteria->add(J046tMarcoNormativoPeer::CO_UPDATED_AT, $this->co_updated_at);
		if ($this->isColumnModified(J046tMarcoNormativoPeer::CREATED_AT)) $criteria->add(J046tMarcoNormativoPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(J046tMarcoNormativoPeer::UPDATE_AT)) $criteria->add(J046tMarcoNormativoPeer::UPDATE_AT, $this->update_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);

		$criteria->add(J046tMarcoNormativoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getCoMarcoNormativo();
	}

	/**
	 * Generic method to set the primary key (co_marco_normativo column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setCoMarcoNormativo($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of J046tMarcoNormativo (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCoMarcoNormativo($this->co_marco_normativo);

		$copyObj->setCoDocumento($this->co_documento);

		$copyObj->setTxCodigoMarco($this->tx_codigo_marco);

		$copyObj->setTxDescripcion($this->tx_descripcion);

		$copyObj->setCoCreatedAt($this->co_created_at);

		$copyObj->setCoUpdatedAt($this->co_updated_at);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdateAt($this->update_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getJ045tReferenciaMarcos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ045tReferenciaMarco($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ050tInformeMarcos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ050tInformeMarco($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     J046tMarcoNormativo Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     J046tMarcoNormativoPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new J046tMarcoNormativoPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collJ045tReferenciaMarcos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ045tReferenciaMarcos()
	 */
	public function clearJ045tReferenciaMarcos()
	{
		$this->collJ045tReferenciaMarcos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ045tReferenciaMarcos collection (array).
	 *
	 * By default this just sets the collJ045tReferenciaMarcos collection to an empty array (like clearcollJ045tReferenciaMarcos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ045tReferenciaMarcos()
	{
		$this->collJ045tReferenciaMarcos = array();
	}

	/**
	 * Gets an array of J045tReferenciaMarco objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo has previously been saved, it will retrieve
	 * related J045tReferenciaMarcos from storage. If this J046tMarcoNormativo is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J045tReferenciaMarco[]
	 * @throws     PropelException
	 */
	public function getJ045tReferenciaMarcos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
			   $this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

				J045tReferenciaMarcoPeer::addSelectColumns($criteria);
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

				J045tReferenciaMarcoPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
					$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;
		return $this->collJ045tReferenciaMarcos;
	}

	/**
	 * Returns the number of related J045tReferenciaMarco objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J045tReferenciaMarco objects.
	 * @throws     PropelException
	 */
	public function countJ045tReferenciaMarcos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

				$count = J045tReferenciaMarcoPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

				if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
					$count = J045tReferenciaMarcoPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ045tReferenciaMarcos);
				}
			} else {
				$count = count($this->collJ045tReferenciaMarcos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J045tReferenciaMarco object to this object
	 * through the J045tReferenciaMarco foreign key attribute.
	 *
	 * @param      J045tReferenciaMarco $l J045tReferenciaMarco
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ045tReferenciaMarco(J045tReferenciaMarco $l)
	{
		if ($this->collJ045tReferenciaMarcos === null) {
			$this->initJ045tReferenciaMarcos();
		}
		if (!in_array($l, $this->collJ045tReferenciaMarcos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ045tReferenciaMarcos, $l);
			$l->setJ046tMarcoNormativo($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo is new, it will return
	 * an empty collection; or if this J046tMarcoNormativo has previously
	 * been saved, it will retrieve related J045tReferenciaMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J046tMarcoNormativo.
	 */
	public function getJ045tReferenciaMarcosJoinJ034tIncidente($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ034tIncidente($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

			if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ034tIncidente($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;

		return $this->collJ045tReferenciaMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo is new, it will return
	 * an empty collection; or if this J046tMarcoNormativo has previously
	 * been saved, it will retrieve related J045tReferenciaMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J046tMarcoNormativo.
	 */
	public function getJ045tReferenciaMarcosJoinJ030tInformeRecomendaciones($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ030tInformeRecomendaciones($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

			if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ030tInformeRecomendaciones($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;

		return $this->collJ045tReferenciaMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo is new, it will return
	 * an empty collection; or if this J046tMarcoNormativo has previously
	 * been saved, it will retrieve related J045tReferenciaMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J046tMarcoNormativo.
	 */
	public function getJ045tReferenciaMarcosJoinJ016tForense($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ016tForense($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

			if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ016tForense($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;

		return $this->collJ045tReferenciaMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo is new, it will return
	 * an empty collection; or if this J046tMarcoNormativo has previously
	 * been saved, it will retrieve related J045tReferenciaMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J046tMarcoNormativo.
	 */
	public function getJ045tReferenciaMarcosJoinJ031tAccionRecomendada($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ031tAccionRecomendada($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J045tReferenciaMarcoPeer::CO_MARCO_NORMATIVO, $this->co_marco_normativo);

			if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ031tAccionRecomendada($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;

		return $this->collJ045tReferenciaMarcos;
	}

	/**
	 * Clears out the collJ050tInformeMarcos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ050tInformeMarcos()
	 */
	public function clearJ050tInformeMarcos()
	{
		$this->collJ050tInformeMarcos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ050tInformeMarcos collection (array).
	 *
	 * By default this just sets the collJ050tInformeMarcos collection to an empty array (like clearcollJ050tInformeMarcos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ050tInformeMarcos()
	{
		$this->collJ050tInformeMarcos = array();
	}

	/**
	 * Gets an array of J050tInformeMarco objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo has previously been saved, it will retrieve
	 * related J050tInformeMarcos from storage. If this J046tMarcoNormativo is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J050tInformeMarco[]
	 * @throws     PropelException
	 */
	public function getJ050tInformeMarcos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ050tInformeMarcos === null) {
			if ($this->isNew()) {
			   $this->collJ050tInformeMarcos = array();
			} else {

				$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

				J050tInformeMarcoPeer::addSelectColumns($criteria);
				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

				J050tInformeMarcoPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ050tInformeMarcoCriteria) || !$this->lastJ050tInformeMarcoCriteria->equals($criteria)) {
					$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ050tInformeMarcoCriteria = $criteria;
		return $this->collJ050tInformeMarcos;
	}

	/**
	 * Returns the number of related J050tInformeMarco objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J050tInformeMarco objects.
	 * @throws     PropelException
	 */
	public function countJ050tInformeMarcos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ050tInformeMarcos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

				$count = J050tInformeMarcoPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

				if (!isset($this->lastJ050tInformeMarcoCriteria) || !$this->lastJ050tInformeMarcoCriteria->equals($criteria)) {
					$count = J050tInformeMarcoPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ050tInformeMarcos);
				}
			} else {
				$count = count($this->collJ050tInformeMarcos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J050tInformeMarco object to this object
	 * through the J050tInformeMarco foreign key attribute.
	 *
	 * @param      J050tInformeMarco $l J050tInformeMarco
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ050tInformeMarco(J050tInformeMarco $l)
	{
		if ($this->collJ050tInformeMarcos === null) {
			$this->initJ050tInformeMarcos();
		}
		if (!in_array($l, $this->collJ050tInformeMarcos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ050tInformeMarcos, $l);
			$l->setJ046tMarcoNormativo($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo is new, it will return
	 * an empty collection; or if this J046tMarcoNormativo has previously
	 * been saved, it will retrieve related J050tInformeMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J046tMarcoNormativo.
	 */
	public function getJ050tInformeMarcosJoinJ051tTipoRevision($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ050tInformeMarcos === null) {
			if ($this->isNew()) {
				$this->collJ050tInformeMarcos = array();
			} else {

				$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelectJoinJ051tTipoRevision($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

			if (!isset($this->lastJ050tInformeMarcoCriteria) || !$this->lastJ050tInformeMarcoCriteria->equals($criteria)) {
				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelectJoinJ051tTipoRevision($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ050tInformeMarcoCriteria = $criteria;

		return $this->collJ050tInformeMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo is new, it will return
	 * an empty collection; or if this J046tMarcoNormativo has previously
	 * been saved, it will retrieve related J050tInformeMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J046tMarcoNormativo.
	 */
	public function getJ050tInformeMarcosJoinJ052tTipoJustificacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ050tInformeMarcos === null) {
			if ($this->isNew()) {
				$this->collJ050tInformeMarcos = array();
			} else {

				$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelectJoinJ052tTipoJustificacion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

			if (!isset($this->lastJ050tInformeMarcoCriteria) || !$this->lastJ050tInformeMarcoCriteria->equals($criteria)) {
				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelectJoinJ052tTipoJustificacion($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ050tInformeMarcoCriteria = $criteria;

		return $this->collJ050tInformeMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo is new, it will return
	 * an empty collection; or if this J046tMarcoNormativo has previously
	 * been saved, it will retrieve related J050tInformeMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J046tMarcoNormativo.
	 */
	public function getJ050tInformeMarcosJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ050tInformeMarcos === null) {
			if ($this->isNew()) {
				$this->collJ050tInformeMarcos = array();
			} else {

				$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

			if (!isset($this->lastJ050tInformeMarcoCriteria) || !$this->lastJ050tInformeMarcoCriteria->equals($criteria)) {
				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ050tInformeMarcoCriteria = $criteria;

		return $this->collJ050tInformeMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J046tMarcoNormativo is new, it will return
	 * an empty collection; or if this J046tMarcoNormativo has previously
	 * been saved, it will retrieve related J050tInformeMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J046tMarcoNormativo.
	 */
	public function getJ050tInformeMarcosJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J046tMarcoNormativoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ050tInformeMarcos === null) {
			if ($this->isNew()) {
				$this->collJ050tInformeMarcos = array();
			} else {

				$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J050tInformeMarcoPeer::CO_TIPO_DOCUMENTO, $this->co_marco_normativo);

			if (!isset($this->lastJ050tInformeMarcoCriteria) || !$this->lastJ050tInformeMarcoCriteria->equals($criteria)) {
				$this->collJ050tInformeMarcos = J050tInformeMarcoPeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ050tInformeMarcoCriteria = $criteria;

		return $this->collJ050tInformeMarcos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collJ045tReferenciaMarcos) {
				foreach ((array) $this->collJ045tReferenciaMarcos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ050tInformeMarcos) {
				foreach ((array) $this->collJ050tInformeMarcos as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collJ045tReferenciaMarcos = null;
		$this->collJ050tInformeMarcos = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseJ046tMarcoNormativo:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseJ046tMarcoNormativo::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseJ046tMarcoNormativo
