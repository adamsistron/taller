<?php

/**
 * Base class that represents a row from the 'j016t_forense' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Wed Nov 11 13:50:04 2015
 *
 * @package    lib.model.om
 */
abstract class BaseJ016tForense extends BaseObject  implements Persistent {


  const PEER = 'J016tForensePeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        J016tForensePeer
	 */
	protected static $peer;

	/**
	 * The value for the co_forense field.
	 * @var        string
	 */
	protected $co_forense;

	/**
	 * The value for the fe_apertura field.
	 * @var        string
	 */
	protected $fe_apertura;

	/**
	 * The value for the fe_cierre field.
	 * @var        string
	 */
	protected $fe_cierre;

	/**
	 * The value for the co_usuario_apertura field.
	 * @var        string
	 */
	protected $co_usuario_apertura;

	/**
	 * The value for the co_usuario_cierre field.
	 * @var        string
	 */
	protected $co_usuario_cierre;

	/**
	 * The value for the co_region field.
	 * @var        string
	 */
	protected $co_region;

	/**
	 * The value for the co_negocio field.
	 * @var        string
	 */
	protected $co_negocio;

	/**
	 * The value for the co_division field.
	 * @var        string
	 */
	protected $co_division;

	/**
	 * The value for the tx_serial field.
	 * @var        string
	 */
	protected $tx_serial;

	/**
	 * The value for the in_abierto field.
	 * @var        boolean
	 */
	protected $in_abierto;

	/**
	 * The value for the tx_descripcion_solicitud field.
	 * @var        string
	 */
	protected $tx_descripcion_solicitud;

	/**
	 * The value for the tx_caso_aaii field.
	 * @var        string
	 */
	protected $tx_caso_aaii;

	/**
	 * The value for the co_alcance_forense field.
	 * @var        string
	 */
	protected $co_alcance_forense;

	/**
	 * The value for the tx_descripcion_entorno field.
	 * @var        string
	 */
	protected $tx_descripcion_entorno;

	/**
	 * The value for the tx_metologia_herramientas field.
	 * @var        string
	 */
	protected $tx_metologia_herramientas;

	/**
	 * The value for the tx_observaciones field.
	 * @var        string
	 */
	protected $tx_observaciones;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the update_at field.
	 * @var        string
	 */
	protected $update_at;

	/**
	 * The value for the co_usuario_created field.
	 * @var        string
	 */
	protected $co_usuario_created;

	/**
	 * The value for the co_usuario_update field.
	 * @var        string
	 */
	protected $co_usuario_update;

	/**
	 * @var        J002tUsuario
	 */
	protected $aJ002tUsuarioRelatedByCoUsuarioApertura;

	/**
	 * @var        J002tUsuario
	 */
	protected $aJ002tUsuarioRelatedByCoUsuarioCierre;

	/**
	 * @var        J007tRegion
	 */
	protected $aJ007tRegion;

	/**
	 * @var        J014tNegocio
	 */
	protected $aJ014tNegocio;

	/**
	 * @var        J008tDivision
	 */
	protected $aJ008tDivision;

	/**
	 * @var        J017tAlcanceForense
	 */
	protected $aJ017tAlcanceForense;

	/**
	 * @var        J002tUsuario
	 */
	protected $aJ002tUsuarioRelatedByCoUsuarioUpdate;

	/**
	 * @var        array J018tInformeForense[] Collection to store aggregation of J018tInformeForense objects.
	 */
	protected $collJ018tInformeForenses;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ018tInformeForenses.
	 */
	private $lastJ018tInformeForenseCriteria = null;

	/**
	 * @var        array J019tParticipantes[] Collection to store aggregation of J019tParticipantes objects.
	 */
	protected $collJ019tParticipantess;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ019tParticipantess.
	 */
	private $lastJ019tParticipantesCriteria = null;

	/**
	 * @var        array J020tActaAcc[] Collection to store aggregation of J020tActaAcc objects.
	 */
	protected $collJ020tActaAccs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ020tActaAccs.
	 */
	private $lastJ020tActaAccCriteria = null;

	/**
	 * @var        array J021tActaAie[] Collection to store aggregation of J021tActaAie objects.
	 */
	protected $collJ021tActaAies;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ021tActaAies.
	 */
	private $lastJ021tActaAieCriteria = null;

	/**
	 * @var        array J022tActaAnie[] Collection to store aggregation of J022tActaAnie objects.
	 */
	protected $collJ022tActaAnies;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ022tActaAnies.
	 */
	private $lastJ022tActaAnieCriteria = null;

	/**
	 * @var        array J031tAccionRecomendada[] Collection to store aggregation of J031tAccionRecomendada objects.
	 */
	protected $collJ031tAccionRecomendadas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ031tAccionRecomendadas.
	 */
	private $lastJ031tAccionRecomendadaCriteria = null;

	/**
	 * @var        array J032tSerialesEnlacesIr[] Collection to store aggregation of J032tSerialesEnlacesIr objects.
	 */
	protected $collJ032tSerialesEnlacesIrs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ032tSerialesEnlacesIrs.
	 */
	private $lastJ032tSerialesEnlacesIrCriteria = null;

	/**
	 * @var        array J045tReferenciaMarco[] Collection to store aggregation of J045tReferenciaMarco objects.
	 */
	protected $collJ045tReferenciaMarcos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJ045tReferenciaMarcos.
	 */
	private $lastJ045tReferenciaMarcoCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseJ016tForense object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
	}

	/**
	 * Get the [co_forense] column value.
	 * 
	 * @return     string
	 */
	public function getCoForense()
	{
		return $this->co_forense;
	}

	/**
	 * Get the [optionally formatted] temporal [fe_apertura] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFeApertura($format = 'Y-m-d')
	{
		if ($this->fe_apertura === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->fe_apertura);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fe_apertura, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [fe_cierre] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFeCierre($format = 'Y-m-d')
	{
		if ($this->fe_cierre === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->fe_cierre);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fe_cierre, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [co_usuario_apertura] column value.
	 * 
	 * @return     string
	 */
	public function getCoUsuarioApertura()
	{
		return $this->co_usuario_apertura;
	}

	/**
	 * Get the [co_usuario_cierre] column value.
	 * 
	 * @return     string
	 */
	public function getCoUsuarioCierre()
	{
		return $this->co_usuario_cierre;
	}

	/**
	 * Get the [co_region] column value.
	 * 
	 * @return     string
	 */
	public function getCoRegion()
	{
		return $this->co_region;
	}

	/**
	 * Get the [co_negocio] column value.
	 * 
	 * @return     string
	 */
	public function getCoNegocio()
	{
		return $this->co_negocio;
	}

	/**
	 * Get the [co_division] column value.
	 * 
	 * @return     string
	 */
	public function getCoDivision()
	{
		return $this->co_division;
	}

	/**
	 * Get the [tx_serial] column value.
	 * 
	 * @return     string
	 */
	public function getTxSerial()
	{
		return $this->tx_serial;
	}

	/**
	 * Get the [in_abierto] column value.
	 * 
	 * @return     boolean
	 */
	public function getInAbierto()
	{
		return $this->in_abierto;
	}

	/**
	 * Get the [tx_descripcion_solicitud] column value.
	 * 
	 * @return     string
	 */
	public function getTxDescripcionSolicitud()
	{
		return $this->tx_descripcion_solicitud;
	}

	/**
	 * Get the [tx_caso_aaii] column value.
	 * 
	 * @return     string
	 */
	public function getTxCasoAaii()
	{
		return $this->tx_caso_aaii;
	}

	/**
	 * Get the [co_alcance_forense] column value.
	 * 
	 * @return     string
	 */
	public function getCoAlcanceForense()
	{
		return $this->co_alcance_forense;
	}

	/**
	 * Get the [tx_descripcion_entorno] column value.
	 * 
	 * @return     string
	 */
	public function getTxDescripcionEntorno()
	{
		return $this->tx_descripcion_entorno;
	}

	/**
	 * Get the [tx_metologia_herramientas] column value.
	 * 
	 * @return     string
	 */
	public function getTxMetologiaHerramientas()
	{
		return $this->tx_metologia_herramientas;
	}

	/**
	 * Get the [tx_observaciones] column value.
	 * 
	 * @return     string
	 */
	public function getTxObservaciones()
	{
		return $this->tx_observaciones;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d')
	{
		if ($this->created_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->created_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [update_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdateAt($format = 'Y-m-d H:i:s')
	{
		if ($this->update_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->update_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->update_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [co_usuario_created] column value.
	 * 
	 * @return     string
	 */
	public function getCoUsuarioCreated()
	{
		return $this->co_usuario_created;
	}

	/**
	 * Get the [co_usuario_update] column value.
	 * 
	 * @return     string
	 */
	public function getCoUsuarioUpdate()
	{
		return $this->co_usuario_update;
	}

	/**
	 * Set the value of [co_forense] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoForense($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_forense !== $v) {
			$this->co_forense = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_FORENSE;
		}

		return $this;
	} // setCoForense()

	/**
	 * Sets the value of [fe_apertura] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setFeApertura($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fe_apertura !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fe_apertura !== null && $tmpDt = new DateTime($this->fe_apertura)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fe_apertura = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = J016tForensePeer::FE_APERTURA;
			}
		} // if either are not null

		return $this;
	} // setFeApertura()

	/**
	 * Sets the value of [fe_cierre] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setFeCierre($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fe_cierre !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fe_cierre !== null && $tmpDt = new DateTime($this->fe_cierre)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fe_cierre = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = J016tForensePeer::FE_CIERRE;
			}
		} // if either are not null

		return $this;
	} // setFeCierre()

	/**
	 * Set the value of [co_usuario_apertura] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoUsuarioApertura($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_usuario_apertura !== $v) {
			$this->co_usuario_apertura = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_USUARIO_APERTURA;
		}

		if ($this->aJ002tUsuarioRelatedByCoUsuarioApertura !== null && $this->aJ002tUsuarioRelatedByCoUsuarioApertura->getCoUsuario() !== $v) {
			$this->aJ002tUsuarioRelatedByCoUsuarioApertura = null;
		}

		return $this;
	} // setCoUsuarioApertura()

	/**
	 * Set the value of [co_usuario_cierre] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoUsuarioCierre($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_usuario_cierre !== $v) {
			$this->co_usuario_cierre = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_USUARIO_CIERRE;
		}

		if ($this->aJ002tUsuarioRelatedByCoUsuarioCierre !== null && $this->aJ002tUsuarioRelatedByCoUsuarioCierre->getCoUsuario() !== $v) {
			$this->aJ002tUsuarioRelatedByCoUsuarioCierre = null;
		}

		return $this;
	} // setCoUsuarioCierre()

	/**
	 * Set the value of [co_region] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoRegion($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_region !== $v) {
			$this->co_region = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_REGION;
		}

		if ($this->aJ007tRegion !== null && $this->aJ007tRegion->getCoRegion() !== $v) {
			$this->aJ007tRegion = null;
		}

		return $this;
	} // setCoRegion()

	/**
	 * Set the value of [co_negocio] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoNegocio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_negocio !== $v) {
			$this->co_negocio = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_NEGOCIO;
		}

		if ($this->aJ014tNegocio !== null && $this->aJ014tNegocio->getCoNegocio() !== $v) {
			$this->aJ014tNegocio = null;
		}

		return $this;
	} // setCoNegocio()

	/**
	 * Set the value of [co_division] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoDivision($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_division !== $v) {
			$this->co_division = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_DIVISION;
		}

		if ($this->aJ008tDivision !== null && $this->aJ008tDivision->getCoDivision() !== $v) {
			$this->aJ008tDivision = null;
		}

		return $this;
	} // setCoDivision()

	/**
	 * Set the value of [tx_serial] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setTxSerial($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_serial !== $v) {
			$this->tx_serial = $v;
			$this->modifiedColumns[] = J016tForensePeer::TX_SERIAL;
		}

		return $this;
	} // setTxSerial()

	/**
	 * Set the value of [in_abierto] column.
	 * 
	 * @param      boolean $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setInAbierto($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->in_abierto !== $v) {
			$this->in_abierto = $v;
			$this->modifiedColumns[] = J016tForensePeer::IN_ABIERTO;
		}

		return $this;
	} // setInAbierto()

	/**
	 * Set the value of [tx_descripcion_solicitud] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setTxDescripcionSolicitud($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_descripcion_solicitud !== $v) {
			$this->tx_descripcion_solicitud = $v;
			$this->modifiedColumns[] = J016tForensePeer::TX_DESCRIPCION_SOLICITUD;
		}

		return $this;
	} // setTxDescripcionSolicitud()

	/**
	 * Set the value of [tx_caso_aaii] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setTxCasoAaii($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_caso_aaii !== $v) {
			$this->tx_caso_aaii = $v;
			$this->modifiedColumns[] = J016tForensePeer::TX_CASO_AAII;
		}

		return $this;
	} // setTxCasoAaii()

	/**
	 * Set the value of [co_alcance_forense] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoAlcanceForense($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_alcance_forense !== $v) {
			$this->co_alcance_forense = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_ALCANCE_FORENSE;
		}

		if ($this->aJ017tAlcanceForense !== null && $this->aJ017tAlcanceForense->getCoAlcanceForense() !== $v) {
			$this->aJ017tAlcanceForense = null;
		}

		return $this;
	} // setCoAlcanceForense()

	/**
	 * Set the value of [tx_descripcion_entorno] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setTxDescripcionEntorno($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_descripcion_entorno !== $v) {
			$this->tx_descripcion_entorno = $v;
			$this->modifiedColumns[] = J016tForensePeer::TX_DESCRIPCION_ENTORNO;
		}

		return $this;
	} // setTxDescripcionEntorno()

	/**
	 * Set the value of [tx_metologia_herramientas] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setTxMetologiaHerramientas($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_metologia_herramientas !== $v) {
			$this->tx_metologia_herramientas = $v;
			$this->modifiedColumns[] = J016tForensePeer::TX_METOLOGIA_HERRAMIENTAS;
		}

		return $this;
	} // setTxMetologiaHerramientas()

	/**
	 * Set the value of [tx_observaciones] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setTxObservaciones($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tx_observaciones !== $v) {
			$this->tx_observaciones = $v;
			$this->modifiedColumns[] = J016tForensePeer::TX_OBSERVACIONES;
		}

		return $this;
	} // setTxObservaciones()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = J016tForensePeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [update_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setUpdateAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->update_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->update_at !== null && $tmpDt = new DateTime($this->update_at)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->update_at = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = J016tForensePeer::UPDATE_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdateAt()

	/**
	 * Set the value of [co_usuario_created] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoUsuarioCreated($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_usuario_created !== $v) {
			$this->co_usuario_created = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_USUARIO_CREATED;
		}

		return $this;
	} // setCoUsuarioCreated()

	/**
	 * Set the value of [co_usuario_update] column.
	 * 
	 * @param      string $v new value
	 * @return     J016tForense The current object (for fluent API support)
	 */
	public function setCoUsuarioUpdate($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->co_usuario_update !== $v) {
			$this->co_usuario_update = $v;
			$this->modifiedColumns[] = J016tForensePeer::CO_USUARIO_UPDATE;
		}

		if ($this->aJ002tUsuarioRelatedByCoUsuarioUpdate !== null && $this->aJ002tUsuarioRelatedByCoUsuarioUpdate->getCoUsuario() !== $v) {
			$this->aJ002tUsuarioRelatedByCoUsuarioUpdate = null;
		}

		return $this;
	} // setCoUsuarioUpdate()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array())) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->co_forense = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->fe_apertura = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->fe_cierre = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->co_usuario_apertura = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->co_usuario_cierre = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->co_region = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->co_negocio = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->co_division = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->tx_serial = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->in_abierto = ($row[$startcol + 9] !== null) ? (boolean) $row[$startcol + 9] : null;
			$this->tx_descripcion_solicitud = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->tx_caso_aaii = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->co_alcance_forense = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->tx_descripcion_entorno = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->tx_metologia_herramientas = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->tx_observaciones = ($row[$startcol + 15] !== null) ? (string) $row[$startcol + 15] : null;
			$this->created_at = ($row[$startcol + 16] !== null) ? (string) $row[$startcol + 16] : null;
			$this->update_at = ($row[$startcol + 17] !== null) ? (string) $row[$startcol + 17] : null;
			$this->co_usuario_created = ($row[$startcol + 18] !== null) ? (string) $row[$startcol + 18] : null;
			$this->co_usuario_update = ($row[$startcol + 19] !== null) ? (string) $row[$startcol + 19] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 20; // 20 = J016tForensePeer::NUM_COLUMNS - J016tForensePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating J016tForense object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aJ002tUsuarioRelatedByCoUsuarioApertura !== null && $this->co_usuario_apertura !== $this->aJ002tUsuarioRelatedByCoUsuarioApertura->getCoUsuario()) {
			$this->aJ002tUsuarioRelatedByCoUsuarioApertura = null;
		}
		if ($this->aJ002tUsuarioRelatedByCoUsuarioCierre !== null && $this->co_usuario_cierre !== $this->aJ002tUsuarioRelatedByCoUsuarioCierre->getCoUsuario()) {
			$this->aJ002tUsuarioRelatedByCoUsuarioCierre = null;
		}
		if ($this->aJ007tRegion !== null && $this->co_region !== $this->aJ007tRegion->getCoRegion()) {
			$this->aJ007tRegion = null;
		}
		if ($this->aJ014tNegocio !== null && $this->co_negocio !== $this->aJ014tNegocio->getCoNegocio()) {
			$this->aJ014tNegocio = null;
		}
		if ($this->aJ008tDivision !== null && $this->co_division !== $this->aJ008tDivision->getCoDivision()) {
			$this->aJ008tDivision = null;
		}
		if ($this->aJ017tAlcanceForense !== null && $this->co_alcance_forense !== $this->aJ017tAlcanceForense->getCoAlcanceForense()) {
			$this->aJ017tAlcanceForense = null;
		}
		if ($this->aJ002tUsuarioRelatedByCoUsuarioUpdate !== null && $this->co_usuario_update !== $this->aJ002tUsuarioRelatedByCoUsuarioUpdate->getCoUsuario()) {
			$this->aJ002tUsuarioRelatedByCoUsuarioUpdate = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J016tForensePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = J016tForensePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aJ002tUsuarioRelatedByCoUsuarioApertura = null;
			$this->aJ002tUsuarioRelatedByCoUsuarioCierre = null;
			$this->aJ007tRegion = null;
			$this->aJ014tNegocio = null;
			$this->aJ008tDivision = null;
			$this->aJ017tAlcanceForense = null;
			$this->aJ002tUsuarioRelatedByCoUsuarioUpdate = null;
			$this->collJ018tInformeForenses = null;
			$this->lastJ018tInformeForenseCriteria = null;

			$this->collJ019tParticipantess = null;
			$this->lastJ019tParticipantesCriteria = null;

			$this->collJ020tActaAccs = null;
			$this->lastJ020tActaAccCriteria = null;

			$this->collJ021tActaAies = null;
			$this->lastJ021tActaAieCriteria = null;

			$this->collJ022tActaAnies = null;
			$this->lastJ022tActaAnieCriteria = null;

			$this->collJ031tAccionRecomendadas = null;
			$this->lastJ031tAccionRecomendadaCriteria = null;

			$this->collJ032tSerialesEnlacesIrs = null;
			$this->lastJ032tSerialesEnlacesIrCriteria = null;

			$this->collJ045tReferenciaMarcos = null;
			$this->lastJ045tReferenciaMarcoCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ016tForense:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J016tForensePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			J016tForensePeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseJ016tForense:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseJ016tForense:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(J016tForensePeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(J016tForensePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseJ016tForense:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			J016tForensePeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aJ002tUsuarioRelatedByCoUsuarioApertura !== null) {
				if ($this->aJ002tUsuarioRelatedByCoUsuarioApertura->isModified() || $this->aJ002tUsuarioRelatedByCoUsuarioApertura->isNew()) {
					$affectedRows += $this->aJ002tUsuarioRelatedByCoUsuarioApertura->save($con);
				}
				$this->setJ002tUsuarioRelatedByCoUsuarioApertura($this->aJ002tUsuarioRelatedByCoUsuarioApertura);
			}

			if ($this->aJ002tUsuarioRelatedByCoUsuarioCierre !== null) {
				if ($this->aJ002tUsuarioRelatedByCoUsuarioCierre->isModified() || $this->aJ002tUsuarioRelatedByCoUsuarioCierre->isNew()) {
					$affectedRows += $this->aJ002tUsuarioRelatedByCoUsuarioCierre->save($con);
				}
				$this->setJ002tUsuarioRelatedByCoUsuarioCierre($this->aJ002tUsuarioRelatedByCoUsuarioCierre);
			}

			if ($this->aJ007tRegion !== null) {
				if ($this->aJ007tRegion->isModified() || $this->aJ007tRegion->isNew()) {
					$affectedRows += $this->aJ007tRegion->save($con);
				}
				$this->setJ007tRegion($this->aJ007tRegion);
			}

			if ($this->aJ014tNegocio !== null) {
				if ($this->aJ014tNegocio->isModified() || $this->aJ014tNegocio->isNew()) {
					$affectedRows += $this->aJ014tNegocio->save($con);
				}
				$this->setJ014tNegocio($this->aJ014tNegocio);
			}

			if ($this->aJ008tDivision !== null) {
				if ($this->aJ008tDivision->isModified() || $this->aJ008tDivision->isNew()) {
					$affectedRows += $this->aJ008tDivision->save($con);
				}
				$this->setJ008tDivision($this->aJ008tDivision);
			}

			if ($this->aJ017tAlcanceForense !== null) {
				if ($this->aJ017tAlcanceForense->isModified() || $this->aJ017tAlcanceForense->isNew()) {
					$affectedRows += $this->aJ017tAlcanceForense->save($con);
				}
				$this->setJ017tAlcanceForense($this->aJ017tAlcanceForense);
			}

			if ($this->aJ002tUsuarioRelatedByCoUsuarioUpdate !== null) {
				if ($this->aJ002tUsuarioRelatedByCoUsuarioUpdate->isModified() || $this->aJ002tUsuarioRelatedByCoUsuarioUpdate->isNew()) {
					$affectedRows += $this->aJ002tUsuarioRelatedByCoUsuarioUpdate->save($con);
				}
				$this->setJ002tUsuarioRelatedByCoUsuarioUpdate($this->aJ002tUsuarioRelatedByCoUsuarioUpdate);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = J016tForensePeer::CO_FORENSE;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = J016tForensePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setCoForense($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += J016tForensePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collJ018tInformeForenses !== null) {
				foreach ($this->collJ018tInformeForenses as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ019tParticipantess !== null) {
				foreach ($this->collJ019tParticipantess as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ020tActaAccs !== null) {
				foreach ($this->collJ020tActaAccs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ021tActaAies !== null) {
				foreach ($this->collJ021tActaAies as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ022tActaAnies !== null) {
				foreach ($this->collJ022tActaAnies as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ031tAccionRecomendadas !== null) {
				foreach ($this->collJ031tAccionRecomendadas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ032tSerialesEnlacesIrs !== null) {
				foreach ($this->collJ032tSerialesEnlacesIrs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collJ045tReferenciaMarcos !== null) {
				foreach ($this->collJ045tReferenciaMarcos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aJ002tUsuarioRelatedByCoUsuarioApertura !== null) {
				if (!$this->aJ002tUsuarioRelatedByCoUsuarioApertura->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJ002tUsuarioRelatedByCoUsuarioApertura->getValidationFailures());
				}
			}

			if ($this->aJ002tUsuarioRelatedByCoUsuarioCierre !== null) {
				if (!$this->aJ002tUsuarioRelatedByCoUsuarioCierre->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJ002tUsuarioRelatedByCoUsuarioCierre->getValidationFailures());
				}
			}

			if ($this->aJ007tRegion !== null) {
				if (!$this->aJ007tRegion->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJ007tRegion->getValidationFailures());
				}
			}

			if ($this->aJ014tNegocio !== null) {
				if (!$this->aJ014tNegocio->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJ014tNegocio->getValidationFailures());
				}
			}

			if ($this->aJ008tDivision !== null) {
				if (!$this->aJ008tDivision->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJ008tDivision->getValidationFailures());
				}
			}

			if ($this->aJ017tAlcanceForense !== null) {
				if (!$this->aJ017tAlcanceForense->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJ017tAlcanceForense->getValidationFailures());
				}
			}

			if ($this->aJ002tUsuarioRelatedByCoUsuarioUpdate !== null) {
				if (!$this->aJ002tUsuarioRelatedByCoUsuarioUpdate->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJ002tUsuarioRelatedByCoUsuarioUpdate->getValidationFailures());
				}
			}


			if (($retval = J016tForensePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collJ018tInformeForenses !== null) {
					foreach ($this->collJ018tInformeForenses as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ019tParticipantess !== null) {
					foreach ($this->collJ019tParticipantess as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ020tActaAccs !== null) {
					foreach ($this->collJ020tActaAccs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ021tActaAies !== null) {
					foreach ($this->collJ021tActaAies as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ022tActaAnies !== null) {
					foreach ($this->collJ022tActaAnies as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ031tAccionRecomendadas !== null) {
					foreach ($this->collJ031tAccionRecomendadas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ032tSerialesEnlacesIrs !== null) {
					foreach ($this->collJ032tSerialesEnlacesIrs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collJ045tReferenciaMarcos !== null) {
					foreach ($this->collJ045tReferenciaMarcos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = J016tForensePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCoForense();
				break;
			case 1:
				return $this->getFeApertura();
				break;
			case 2:
				return $this->getFeCierre();
				break;
			case 3:
				return $this->getCoUsuarioApertura();
				break;
			case 4:
				return $this->getCoUsuarioCierre();
				break;
			case 5:
				return $this->getCoRegion();
				break;
			case 6:
				return $this->getCoNegocio();
				break;
			case 7:
				return $this->getCoDivision();
				break;
			case 8:
				return $this->getTxSerial();
				break;
			case 9:
				return $this->getInAbierto();
				break;
			case 10:
				return $this->getTxDescripcionSolicitud();
				break;
			case 11:
				return $this->getTxCasoAaii();
				break;
			case 12:
				return $this->getCoAlcanceForense();
				break;
			case 13:
				return $this->getTxDescripcionEntorno();
				break;
			case 14:
				return $this->getTxMetologiaHerramientas();
				break;
			case 15:
				return $this->getTxObservaciones();
				break;
			case 16:
				return $this->getCreatedAt();
				break;
			case 17:
				return $this->getUpdateAt();
				break;
			case 18:
				return $this->getCoUsuarioCreated();
				break;
			case 19:
				return $this->getCoUsuarioUpdate();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = J016tForensePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCoForense(),
			$keys[1] => $this->getFeApertura(),
			$keys[2] => $this->getFeCierre(),
			$keys[3] => $this->getCoUsuarioApertura(),
			$keys[4] => $this->getCoUsuarioCierre(),
			$keys[5] => $this->getCoRegion(),
			$keys[6] => $this->getCoNegocio(),
			$keys[7] => $this->getCoDivision(),
			$keys[8] => $this->getTxSerial(),
			$keys[9] => $this->getInAbierto(),
			$keys[10] => $this->getTxDescripcionSolicitud(),
			$keys[11] => $this->getTxCasoAaii(),
			$keys[12] => $this->getCoAlcanceForense(),
			$keys[13] => $this->getTxDescripcionEntorno(),
			$keys[14] => $this->getTxMetologiaHerramientas(),
			$keys[15] => $this->getTxObservaciones(),
			$keys[16] => $this->getCreatedAt(),
			$keys[17] => $this->getUpdateAt(),
			$keys[18] => $this->getCoUsuarioCreated(),
			$keys[19] => $this->getCoUsuarioUpdate(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = J016tForensePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCoForense($value);
				break;
			case 1:
				$this->setFeApertura($value);
				break;
			case 2:
				$this->setFeCierre($value);
				break;
			case 3:
				$this->setCoUsuarioApertura($value);
				break;
			case 4:
				$this->setCoUsuarioCierre($value);
				break;
			case 5:
				$this->setCoRegion($value);
				break;
			case 6:
				$this->setCoNegocio($value);
				break;
			case 7:
				$this->setCoDivision($value);
				break;
			case 8:
				$this->setTxSerial($value);
				break;
			case 9:
				$this->setInAbierto($value);
				break;
			case 10:
				$this->setTxDescripcionSolicitud($value);
				break;
			case 11:
				$this->setTxCasoAaii($value);
				break;
			case 12:
				$this->setCoAlcanceForense($value);
				break;
			case 13:
				$this->setTxDescripcionEntorno($value);
				break;
			case 14:
				$this->setTxMetologiaHerramientas($value);
				break;
			case 15:
				$this->setTxObservaciones($value);
				break;
			case 16:
				$this->setCreatedAt($value);
				break;
			case 17:
				$this->setUpdateAt($value);
				break;
			case 18:
				$this->setCoUsuarioCreated($value);
				break;
			case 19:
				$this->setCoUsuarioUpdate($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = J016tForensePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCoForense($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setFeApertura($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setFeCierre($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCoUsuarioApertura($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCoUsuarioCierre($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCoRegion($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCoNegocio($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setCoDivision($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setTxSerial($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setInAbierto($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setTxDescripcionSolicitud($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setTxCasoAaii($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setCoAlcanceForense($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setTxDescripcionEntorno($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setTxMetologiaHerramientas($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setTxObservaciones($arr[$keys[15]]);
		if (array_key_exists($keys[16], $arr)) $this->setCreatedAt($arr[$keys[16]]);
		if (array_key_exists($keys[17], $arr)) $this->setUpdateAt($arr[$keys[17]]);
		if (array_key_exists($keys[18], $arr)) $this->setCoUsuarioCreated($arr[$keys[18]]);
		if (array_key_exists($keys[19], $arr)) $this->setCoUsuarioUpdate($arr[$keys[19]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);

		if ($this->isColumnModified(J016tForensePeer::CO_FORENSE)) $criteria->add(J016tForensePeer::CO_FORENSE, $this->co_forense);
		if ($this->isColumnModified(J016tForensePeer::FE_APERTURA)) $criteria->add(J016tForensePeer::FE_APERTURA, $this->fe_apertura);
		if ($this->isColumnModified(J016tForensePeer::FE_CIERRE)) $criteria->add(J016tForensePeer::FE_CIERRE, $this->fe_cierre);
		if ($this->isColumnModified(J016tForensePeer::CO_USUARIO_APERTURA)) $criteria->add(J016tForensePeer::CO_USUARIO_APERTURA, $this->co_usuario_apertura);
		if ($this->isColumnModified(J016tForensePeer::CO_USUARIO_CIERRE)) $criteria->add(J016tForensePeer::CO_USUARIO_CIERRE, $this->co_usuario_cierre);
		if ($this->isColumnModified(J016tForensePeer::CO_REGION)) $criteria->add(J016tForensePeer::CO_REGION, $this->co_region);
		if ($this->isColumnModified(J016tForensePeer::CO_NEGOCIO)) $criteria->add(J016tForensePeer::CO_NEGOCIO, $this->co_negocio);
		if ($this->isColumnModified(J016tForensePeer::CO_DIVISION)) $criteria->add(J016tForensePeer::CO_DIVISION, $this->co_division);
		if ($this->isColumnModified(J016tForensePeer::TX_SERIAL)) $criteria->add(J016tForensePeer::TX_SERIAL, $this->tx_serial);
		if ($this->isColumnModified(J016tForensePeer::IN_ABIERTO)) $criteria->add(J016tForensePeer::IN_ABIERTO, $this->in_abierto);
		if ($this->isColumnModified(J016tForensePeer::TX_DESCRIPCION_SOLICITUD)) $criteria->add(J016tForensePeer::TX_DESCRIPCION_SOLICITUD, $this->tx_descripcion_solicitud);
		if ($this->isColumnModified(J016tForensePeer::TX_CASO_AAII)) $criteria->add(J016tForensePeer::TX_CASO_AAII, $this->tx_caso_aaii);
		if ($this->isColumnModified(J016tForensePeer::CO_ALCANCE_FORENSE)) $criteria->add(J016tForensePeer::CO_ALCANCE_FORENSE, $this->co_alcance_forense);
		if ($this->isColumnModified(J016tForensePeer::TX_DESCRIPCION_ENTORNO)) $criteria->add(J016tForensePeer::TX_DESCRIPCION_ENTORNO, $this->tx_descripcion_entorno);
		if ($this->isColumnModified(J016tForensePeer::TX_METOLOGIA_HERRAMIENTAS)) $criteria->add(J016tForensePeer::TX_METOLOGIA_HERRAMIENTAS, $this->tx_metologia_herramientas);
		if ($this->isColumnModified(J016tForensePeer::TX_OBSERVACIONES)) $criteria->add(J016tForensePeer::TX_OBSERVACIONES, $this->tx_observaciones);
		if ($this->isColumnModified(J016tForensePeer::CREATED_AT)) $criteria->add(J016tForensePeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(J016tForensePeer::UPDATE_AT)) $criteria->add(J016tForensePeer::UPDATE_AT, $this->update_at);
		if ($this->isColumnModified(J016tForensePeer::CO_USUARIO_CREATED)) $criteria->add(J016tForensePeer::CO_USUARIO_CREATED, $this->co_usuario_created);
		if ($this->isColumnModified(J016tForensePeer::CO_USUARIO_UPDATE)) $criteria->add(J016tForensePeer::CO_USUARIO_UPDATE, $this->co_usuario_update);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);

		$criteria->add(J016tForensePeer::CO_FORENSE, $this->co_forense);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getCoForense();
	}

	/**
	 * Generic method to set the primary key (co_forense column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setCoForense($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of J016tForense (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setFeApertura($this->fe_apertura);

		$copyObj->setFeCierre($this->fe_cierre);

		$copyObj->setCoUsuarioApertura($this->co_usuario_apertura);

		$copyObj->setCoUsuarioCierre($this->co_usuario_cierre);

		$copyObj->setCoRegion($this->co_region);

		$copyObj->setCoNegocio($this->co_negocio);

		$copyObj->setCoDivision($this->co_division);

		$copyObj->setTxSerial($this->tx_serial);

		$copyObj->setInAbierto($this->in_abierto);

		$copyObj->setTxDescripcionSolicitud($this->tx_descripcion_solicitud);

		$copyObj->setTxCasoAaii($this->tx_caso_aaii);

		$copyObj->setCoAlcanceForense($this->co_alcance_forense);

		$copyObj->setTxDescripcionEntorno($this->tx_descripcion_entorno);

		$copyObj->setTxMetologiaHerramientas($this->tx_metologia_herramientas);

		$copyObj->setTxObservaciones($this->tx_observaciones);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdateAt($this->update_at);

		$copyObj->setCoUsuarioCreated($this->co_usuario_created);

		$copyObj->setCoUsuarioUpdate($this->co_usuario_update);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getJ018tInformeForenses() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ018tInformeForense($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ019tParticipantess() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ019tParticipantes($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ020tActaAccs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ020tActaAcc($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ021tActaAies() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ021tActaAie($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ022tActaAnies() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ022tActaAnie($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ031tAccionRecomendadas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ031tAccionRecomendada($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ032tSerialesEnlacesIrs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ032tSerialesEnlacesIr($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getJ045tReferenciaMarcos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJ045tReferenciaMarco($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setCoForense(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     J016tForense Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     J016tForensePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new J016tForensePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a J002tUsuario object.
	 *
	 * @param      J002tUsuario $v
	 * @return     J016tForense The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJ002tUsuarioRelatedByCoUsuarioApertura(J002tUsuario $v = null)
	{
		if ($v === null) {
			$this->setCoUsuarioApertura(NULL);
		} else {
			$this->setCoUsuarioApertura($v->getCoUsuario());
		}

		$this->aJ002tUsuarioRelatedByCoUsuarioApertura = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the J002tUsuario object, it will not be re-added.
		if ($v !== null) {
			$v->addJ016tForenseRelatedByCoUsuarioApertura($this);
		}

		return $this;
	}


	/**
	 * Get the associated J002tUsuario object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     J002tUsuario The associated J002tUsuario object.
	 * @throws     PropelException
	 */
	public function getJ002tUsuarioRelatedByCoUsuarioApertura(PropelPDO $con = null)
	{
		if ($this->aJ002tUsuarioRelatedByCoUsuarioApertura === null && (($this->co_usuario_apertura !== "" && $this->co_usuario_apertura !== null))) {
			$c = new Criteria(J002tUsuarioPeer::DATABASE_NAME);
			$c->add(J002tUsuarioPeer::CO_USUARIO, $this->co_usuario_apertura);
			$this->aJ002tUsuarioRelatedByCoUsuarioApertura = J002tUsuarioPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJ002tUsuarioRelatedByCoUsuarioApertura->addJ016tForensesRelatedByCoUsuarioApertura($this);
			 */
		}
		return $this->aJ002tUsuarioRelatedByCoUsuarioApertura;
	}

	/**
	 * Declares an association between this object and a J002tUsuario object.
	 *
	 * @param      J002tUsuario $v
	 * @return     J016tForense The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJ002tUsuarioRelatedByCoUsuarioCierre(J002tUsuario $v = null)
	{
		if ($v === null) {
			$this->setCoUsuarioCierre(NULL);
		} else {
			$this->setCoUsuarioCierre($v->getCoUsuario());
		}

		$this->aJ002tUsuarioRelatedByCoUsuarioCierre = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the J002tUsuario object, it will not be re-added.
		if ($v !== null) {
			$v->addJ016tForenseRelatedByCoUsuarioCierre($this);
		}

		return $this;
	}


	/**
	 * Get the associated J002tUsuario object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     J002tUsuario The associated J002tUsuario object.
	 * @throws     PropelException
	 */
	public function getJ002tUsuarioRelatedByCoUsuarioCierre(PropelPDO $con = null)
	{
		if ($this->aJ002tUsuarioRelatedByCoUsuarioCierre === null && (($this->co_usuario_cierre !== "" && $this->co_usuario_cierre !== null))) {
			$c = new Criteria(J002tUsuarioPeer::DATABASE_NAME);
			$c->add(J002tUsuarioPeer::CO_USUARIO, $this->co_usuario_cierre);
			$this->aJ002tUsuarioRelatedByCoUsuarioCierre = J002tUsuarioPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJ002tUsuarioRelatedByCoUsuarioCierre->addJ016tForensesRelatedByCoUsuarioCierre($this);
			 */
		}
		return $this->aJ002tUsuarioRelatedByCoUsuarioCierre;
	}

	/**
	 * Declares an association between this object and a J007tRegion object.
	 *
	 * @param      J007tRegion $v
	 * @return     J016tForense The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJ007tRegion(J007tRegion $v = null)
	{
		if ($v === null) {
			$this->setCoRegion(NULL);
		} else {
			$this->setCoRegion($v->getCoRegion());
		}

		$this->aJ007tRegion = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the J007tRegion object, it will not be re-added.
		if ($v !== null) {
			$v->addJ016tForense($this);
		}

		return $this;
	}


	/**
	 * Get the associated J007tRegion object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     J007tRegion The associated J007tRegion object.
	 * @throws     PropelException
	 */
	public function getJ007tRegion(PropelPDO $con = null)
	{
		if ($this->aJ007tRegion === null && (($this->co_region !== "" && $this->co_region !== null))) {
			$c = new Criteria(J007tRegionPeer::DATABASE_NAME);
			$c->add(J007tRegionPeer::CO_REGION, $this->co_region);
			$this->aJ007tRegion = J007tRegionPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJ007tRegion->addJ016tForenses($this);
			 */
		}
		return $this->aJ007tRegion;
	}

	/**
	 * Declares an association between this object and a J014tNegocio object.
	 *
	 * @param      J014tNegocio $v
	 * @return     J016tForense The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJ014tNegocio(J014tNegocio $v = null)
	{
		if ($v === null) {
			$this->setCoNegocio(NULL);
		} else {
			$this->setCoNegocio($v->getCoNegocio());
		}

		$this->aJ014tNegocio = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the J014tNegocio object, it will not be re-added.
		if ($v !== null) {
			$v->addJ016tForense($this);
		}

		return $this;
	}


	/**
	 * Get the associated J014tNegocio object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     J014tNegocio The associated J014tNegocio object.
	 * @throws     PropelException
	 */
	public function getJ014tNegocio(PropelPDO $con = null)
	{
		if ($this->aJ014tNegocio === null && (($this->co_negocio !== "" && $this->co_negocio !== null))) {
			$c = new Criteria(J014tNegocioPeer::DATABASE_NAME);
			$c->add(J014tNegocioPeer::CO_NEGOCIO, $this->co_negocio);
			$this->aJ014tNegocio = J014tNegocioPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJ014tNegocio->addJ016tForenses($this);
			 */
		}
		return $this->aJ014tNegocio;
	}

	/**
	 * Declares an association between this object and a J008tDivision object.
	 *
	 * @param      J008tDivision $v
	 * @return     J016tForense The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJ008tDivision(J008tDivision $v = null)
	{
		if ($v === null) {
			$this->setCoDivision(NULL);
		} else {
			$this->setCoDivision($v->getCoDivision());
		}

		$this->aJ008tDivision = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the J008tDivision object, it will not be re-added.
		if ($v !== null) {
			$v->addJ016tForense($this);
		}

		return $this;
	}


	/**
	 * Get the associated J008tDivision object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     J008tDivision The associated J008tDivision object.
	 * @throws     PropelException
	 */
	public function getJ008tDivision(PropelPDO $con = null)
	{
		if ($this->aJ008tDivision === null && (($this->co_division !== "" && $this->co_division !== null))) {
			$c = new Criteria(J008tDivisionPeer::DATABASE_NAME);
			$c->add(J008tDivisionPeer::CO_DIVISION, $this->co_division);
			$this->aJ008tDivision = J008tDivisionPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJ008tDivision->addJ016tForenses($this);
			 */
		}
		return $this->aJ008tDivision;
	}

	/**
	 * Declares an association between this object and a J017tAlcanceForense object.
	 *
	 * @param      J017tAlcanceForense $v
	 * @return     J016tForense The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJ017tAlcanceForense(J017tAlcanceForense $v = null)
	{
		if ($v === null) {
			$this->setCoAlcanceForense(NULL);
		} else {
			$this->setCoAlcanceForense($v->getCoAlcanceForense());
		}

		$this->aJ017tAlcanceForense = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the J017tAlcanceForense object, it will not be re-added.
		if ($v !== null) {
			$v->addJ016tForense($this);
		}

		return $this;
	}


	/**
	 * Get the associated J017tAlcanceForense object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     J017tAlcanceForense The associated J017tAlcanceForense object.
	 * @throws     PropelException
	 */
	public function getJ017tAlcanceForense(PropelPDO $con = null)
	{
		if ($this->aJ017tAlcanceForense === null && (($this->co_alcance_forense !== "" && $this->co_alcance_forense !== null))) {
			$c = new Criteria(J017tAlcanceForensePeer::DATABASE_NAME);
			$c->add(J017tAlcanceForensePeer::CO_ALCANCE_FORENSE, $this->co_alcance_forense);
			$this->aJ017tAlcanceForense = J017tAlcanceForensePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJ017tAlcanceForense->addJ016tForenses($this);
			 */
		}
		return $this->aJ017tAlcanceForense;
	}

	/**
	 * Declares an association between this object and a J002tUsuario object.
	 *
	 * @param      J002tUsuario $v
	 * @return     J016tForense The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJ002tUsuarioRelatedByCoUsuarioUpdate(J002tUsuario $v = null)
	{
		if ($v === null) {
			$this->setCoUsuarioUpdate(NULL);
		} else {
			$this->setCoUsuarioUpdate($v->getCoUsuario());
		}

		$this->aJ002tUsuarioRelatedByCoUsuarioUpdate = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the J002tUsuario object, it will not be re-added.
		if ($v !== null) {
			$v->addJ016tForenseRelatedByCoUsuarioUpdate($this);
		}

		return $this;
	}


	/**
	 * Get the associated J002tUsuario object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     J002tUsuario The associated J002tUsuario object.
	 * @throws     PropelException
	 */
	public function getJ002tUsuarioRelatedByCoUsuarioUpdate(PropelPDO $con = null)
	{
		if ($this->aJ002tUsuarioRelatedByCoUsuarioUpdate === null && (($this->co_usuario_update !== "" && $this->co_usuario_update !== null))) {
			$c = new Criteria(J002tUsuarioPeer::DATABASE_NAME);
			$c->add(J002tUsuarioPeer::CO_USUARIO, $this->co_usuario_update);
			$this->aJ002tUsuarioRelatedByCoUsuarioUpdate = J002tUsuarioPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJ002tUsuarioRelatedByCoUsuarioUpdate->addJ016tForensesRelatedByCoUsuarioUpdate($this);
			 */
		}
		return $this->aJ002tUsuarioRelatedByCoUsuarioUpdate;
	}

	/**
	 * Clears out the collJ018tInformeForenses collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ018tInformeForenses()
	 */
	public function clearJ018tInformeForenses()
	{
		$this->collJ018tInformeForenses = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ018tInformeForenses collection (array).
	 *
	 * By default this just sets the collJ018tInformeForenses collection to an empty array (like clearcollJ018tInformeForenses());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ018tInformeForenses()
	{
		$this->collJ018tInformeForenses = array();
	}

	/**
	 * Gets an array of J018tInformeForense objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J016tForense has previously been saved, it will retrieve
	 * related J018tInformeForenses from storage. If this J016tForense is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J018tInformeForense[]
	 * @throws     PropelException
	 */
	public function getJ018tInformeForenses($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
			   $this->collJ018tInformeForenses = array();
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				J018tInformeForensePeer::addSelectColumns($criteria);
				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				J018tInformeForensePeer::addSelectColumns($criteria);
				if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
					$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ018tInformeForenseCriteria = $criteria;
		return $this->collJ018tInformeForenses;
	}

	/**
	 * Returns the number of related J018tInformeForense objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J018tInformeForense objects.
	 * @throws     PropelException
	 */
	public function countJ018tInformeForenses(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				$count = J018tInformeForensePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
					$count = J018tInformeForensePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ018tInformeForenses);
				}
			} else {
				$count = count($this->collJ018tInformeForenses);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J018tInformeForense object to this object
	 * through the J018tInformeForense foreign key attribute.
	 *
	 * @param      J018tInformeForense $l J018tInformeForense
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ018tInformeForense(J018tInformeForense $l)
	{
		if ($this->collJ018tInformeForenses === null) {
			$this->initJ018tInformeForenses();
		}
		if (!in_array($l, $this->collJ018tInformeForenses, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ018tInformeForenses, $l);
			$l->setJ016tForense($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J018tInformeForenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ018tInformeForensesJoinJ007tRegion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
				$this->collJ018tInformeForenses = array();
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ018tInformeForenseCriteria = $criteria;

		return $this->collJ018tInformeForenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J018tInformeForenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ018tInformeForensesJoinJ014tNegocio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
				$this->collJ018tInformeForenses = array();
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ014tNegocio($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ014tNegocio($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ018tInformeForenseCriteria = $criteria;

		return $this->collJ018tInformeForenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J018tInformeForenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ018tInformeForensesJoinJ008tDivision($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
				$this->collJ018tInformeForenses = array();
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ008tDivision($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ008tDivision($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ018tInformeForenseCriteria = $criteria;

		return $this->collJ018tInformeForenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J018tInformeForenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ018tInformeForensesJoinJ002tUsuarioRelatedByCoRevisa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
				$this->collJ018tInformeForenses = array();
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ002tUsuarioRelatedByCoRevisa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ002tUsuarioRelatedByCoRevisa($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ018tInformeForenseCriteria = $criteria;

		return $this->collJ018tInformeForenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J018tInformeForenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ018tInformeForensesJoinJ002tUsuarioRelatedByCoAprueba($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
				$this->collJ018tInformeForenses = array();
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ002tUsuarioRelatedByCoAprueba($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ002tUsuarioRelatedByCoAprueba($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ018tInformeForenseCriteria = $criteria;

		return $this->collJ018tInformeForenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J018tInformeForenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ018tInformeForensesJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
				$this->collJ018tInformeForenses = array();
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ018tInformeForenseCriteria = $criteria;

		return $this->collJ018tInformeForenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J018tInformeForenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ018tInformeForensesJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ018tInformeForenses === null) {
			if ($this->isNew()) {
				$this->collJ018tInformeForenses = array();
			} else {

				$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J018tInformeForensePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ018tInformeForenseCriteria) || !$this->lastJ018tInformeForenseCriteria->equals($criteria)) {
				$this->collJ018tInformeForenses = J018tInformeForensePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ018tInformeForenseCriteria = $criteria;

		return $this->collJ018tInformeForenses;
	}

	/**
	 * Clears out the collJ019tParticipantess collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ019tParticipantess()
	 */
	public function clearJ019tParticipantess()
	{
		$this->collJ019tParticipantess = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ019tParticipantess collection (array).
	 *
	 * By default this just sets the collJ019tParticipantess collection to an empty array (like clearcollJ019tParticipantess());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ019tParticipantess()
	{
		$this->collJ019tParticipantess = array();
	}

	/**
	 * Gets an array of J019tParticipantes objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J016tForense has previously been saved, it will retrieve
	 * related J019tParticipantess from storage. If this J016tForense is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J019tParticipantes[]
	 * @throws     PropelException
	 */
	public function getJ019tParticipantess($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ019tParticipantess === null) {
			if ($this->isNew()) {
			   $this->collJ019tParticipantess = array();
			} else {

				$criteria->add(J019tParticipantesPeer::CO_FORENSE, $this->co_forense);

				J019tParticipantesPeer::addSelectColumns($criteria);
				$this->collJ019tParticipantess = J019tParticipantesPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J019tParticipantesPeer::CO_FORENSE, $this->co_forense);

				J019tParticipantesPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ019tParticipantesCriteria) || !$this->lastJ019tParticipantesCriteria->equals($criteria)) {
					$this->collJ019tParticipantess = J019tParticipantesPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ019tParticipantesCriteria = $criteria;
		return $this->collJ019tParticipantess;
	}

	/**
	 * Returns the number of related J019tParticipantes objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J019tParticipantes objects.
	 * @throws     PropelException
	 */
	public function countJ019tParticipantess(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ019tParticipantess === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J019tParticipantesPeer::CO_FORENSE, $this->co_forense);

				$count = J019tParticipantesPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J019tParticipantesPeer::CO_FORENSE, $this->co_forense);

				if (!isset($this->lastJ019tParticipantesCriteria) || !$this->lastJ019tParticipantesCriteria->equals($criteria)) {
					$count = J019tParticipantesPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ019tParticipantess);
				}
			} else {
				$count = count($this->collJ019tParticipantess);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J019tParticipantes object to this object
	 * through the J019tParticipantes foreign key attribute.
	 *
	 * @param      J019tParticipantes $l J019tParticipantes
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ019tParticipantes(J019tParticipantes $l)
	{
		if ($this->collJ019tParticipantess === null) {
			$this->initJ019tParticipantess();
		}
		if (!in_array($l, $this->collJ019tParticipantess, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ019tParticipantess, $l);
			$l->setJ016tForense($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J019tParticipantess from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ019tParticipantessJoinJ002tUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ019tParticipantess === null) {
			if ($this->isNew()) {
				$this->collJ019tParticipantess = array();
			} else {

				$criteria->add(J019tParticipantesPeer::CO_FORENSE, $this->co_forense);

				$this->collJ019tParticipantess = J019tParticipantesPeer::doSelectJoinJ002tUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J019tParticipantesPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ019tParticipantesCriteria) || !$this->lastJ019tParticipantesCriteria->equals($criteria)) {
				$this->collJ019tParticipantess = J019tParticipantesPeer::doSelectJoinJ002tUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ019tParticipantesCriteria = $criteria;

		return $this->collJ019tParticipantess;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J019tParticipantess from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ019tParticipantessJoinJ018tInformeForense($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ019tParticipantess === null) {
			if ($this->isNew()) {
				$this->collJ019tParticipantess = array();
			} else {

				$criteria->add(J019tParticipantesPeer::CO_FORENSE, $this->co_forense);

				$this->collJ019tParticipantess = J019tParticipantesPeer::doSelectJoinJ018tInformeForense($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J019tParticipantesPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ019tParticipantesCriteria) || !$this->lastJ019tParticipantesCriteria->equals($criteria)) {
				$this->collJ019tParticipantess = J019tParticipantesPeer::doSelectJoinJ018tInformeForense($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ019tParticipantesCriteria = $criteria;

		return $this->collJ019tParticipantess;
	}

	/**
	 * Clears out the collJ020tActaAccs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ020tActaAccs()
	 */
	public function clearJ020tActaAccs()
	{
		$this->collJ020tActaAccs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ020tActaAccs collection (array).
	 *
	 * By default this just sets the collJ020tActaAccs collection to an empty array (like clearcollJ020tActaAccs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ020tActaAccs()
	{
		$this->collJ020tActaAccs = array();
	}

	/**
	 * Gets an array of J020tActaAcc objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J016tForense has previously been saved, it will retrieve
	 * related J020tActaAccs from storage. If this J016tForense is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J020tActaAcc[]
	 * @throws     PropelException
	 */
	public function getJ020tActaAccs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
			   $this->collJ020tActaAccs = array();
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				J020tActaAccPeer::addSelectColumns($criteria);
				$this->collJ020tActaAccs = J020tActaAccPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				J020tActaAccPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
					$this->collJ020tActaAccs = J020tActaAccPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ020tActaAccCriteria = $criteria;
		return $this->collJ020tActaAccs;
	}

	/**
	 * Returns the number of related J020tActaAcc objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J020tActaAcc objects.
	 * @throws     PropelException
	 */
	public function countJ020tActaAccs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				$count = J020tActaAccPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
					$count = J020tActaAccPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ020tActaAccs);
				}
			} else {
				$count = count($this->collJ020tActaAccs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J020tActaAcc object to this object
	 * through the J020tActaAcc foreign key attribute.
	 *
	 * @param      J020tActaAcc $l J020tActaAcc
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ020tActaAcc(J020tActaAcc $l)
	{
		if ($this->collJ020tActaAccs === null) {
			$this->initJ020tActaAccs();
		}
		if (!in_array($l, $this->collJ020tActaAccs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ020tActaAccs, $l);
			$l->setJ016tForense($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J020tActaAccs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ020tActaAccsJoinJ007tRegion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
				$this->collJ020tActaAccs = array();
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ020tActaAccCriteria = $criteria;

		return $this->collJ020tActaAccs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J020tActaAccs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ020tActaAccsJoinJ014tNegocio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
				$this->collJ020tActaAccs = array();
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ014tNegocio($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ014tNegocio($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ020tActaAccCriteria = $criteria;

		return $this->collJ020tActaAccs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J020tActaAccs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ020tActaAccsJoinJ008tDivision($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
				$this->collJ020tActaAccs = array();
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ008tDivision($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ008tDivision($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ020tActaAccCriteria = $criteria;

		return $this->collJ020tActaAccs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J020tActaAccs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ020tActaAccsJoinJ002tUsuarioRelatedByCoElabora($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
				$this->collJ020tActaAccs = array();
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ002tUsuarioRelatedByCoElabora($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ002tUsuarioRelatedByCoElabora($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ020tActaAccCriteria = $criteria;

		return $this->collJ020tActaAccs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J020tActaAccs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ020tActaAccsJoinJ002tUsuarioRelatedByCoDestruye($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
				$this->collJ020tActaAccs = array();
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ002tUsuarioRelatedByCoDestruye($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ002tUsuarioRelatedByCoDestruye($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ020tActaAccCriteria = $criteria;

		return $this->collJ020tActaAccs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J020tActaAccs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ020tActaAccsJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
				$this->collJ020tActaAccs = array();
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ020tActaAccCriteria = $criteria;

		return $this->collJ020tActaAccs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J020tActaAccs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ020tActaAccsJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ020tActaAccs === null) {
			if ($this->isNew()) {
				$this->collJ020tActaAccs = array();
			} else {

				$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J020tActaAccPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ020tActaAccCriteria) || !$this->lastJ020tActaAccCriteria->equals($criteria)) {
				$this->collJ020tActaAccs = J020tActaAccPeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ020tActaAccCriteria = $criteria;

		return $this->collJ020tActaAccs;
	}

	/**
	 * Clears out the collJ021tActaAies collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ021tActaAies()
	 */
	public function clearJ021tActaAies()
	{
		$this->collJ021tActaAies = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ021tActaAies collection (array).
	 *
	 * By default this just sets the collJ021tActaAies collection to an empty array (like clearcollJ021tActaAies());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ021tActaAies()
	{
		$this->collJ021tActaAies = array();
	}

	/**
	 * Gets an array of J021tActaAie objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J016tForense has previously been saved, it will retrieve
	 * related J021tActaAies from storage. If this J016tForense is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J021tActaAie[]
	 * @throws     PropelException
	 */
	public function getJ021tActaAies($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ021tActaAies === null) {
			if ($this->isNew()) {
			   $this->collJ021tActaAies = array();
			} else {

				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				J021tActaAiePeer::addSelectColumns($criteria);
				$this->collJ021tActaAies = J021tActaAiePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				J021tActaAiePeer::addSelectColumns($criteria);
				if (!isset($this->lastJ021tActaAieCriteria) || !$this->lastJ021tActaAieCriteria->equals($criteria)) {
					$this->collJ021tActaAies = J021tActaAiePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ021tActaAieCriteria = $criteria;
		return $this->collJ021tActaAies;
	}

	/**
	 * Returns the number of related J021tActaAie objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J021tActaAie objects.
	 * @throws     PropelException
	 */
	public function countJ021tActaAies(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ021tActaAies === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				$count = J021tActaAiePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				if (!isset($this->lastJ021tActaAieCriteria) || !$this->lastJ021tActaAieCriteria->equals($criteria)) {
					$count = J021tActaAiePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ021tActaAies);
				}
			} else {
				$count = count($this->collJ021tActaAies);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J021tActaAie object to this object
	 * through the J021tActaAie foreign key attribute.
	 *
	 * @param      J021tActaAie $l J021tActaAie
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ021tActaAie(J021tActaAie $l)
	{
		if ($this->collJ021tActaAies === null) {
			$this->initJ021tActaAies();
		}
		if (!in_array($l, $this->collJ021tActaAies, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ021tActaAies, $l);
			$l->setJ016tForense($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J021tActaAies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ021tActaAiesJoinJ007tRegion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ021tActaAies === null) {
			if ($this->isNew()) {
				$this->collJ021tActaAies = array();
			} else {

				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ021tActaAieCriteria) || !$this->lastJ021tActaAieCriteria->equals($criteria)) {
				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ021tActaAieCriteria = $criteria;

		return $this->collJ021tActaAies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J021tActaAies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ021tActaAiesJoinJ014tNegocio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ021tActaAies === null) {
			if ($this->isNew()) {
				$this->collJ021tActaAies = array();
			} else {

				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ014tNegocio($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ021tActaAieCriteria) || !$this->lastJ021tActaAieCriteria->equals($criteria)) {
				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ014tNegocio($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ021tActaAieCriteria = $criteria;

		return $this->collJ021tActaAies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J021tActaAies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ021tActaAiesJoinJ008tDivision($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ021tActaAies === null) {
			if ($this->isNew()) {
				$this->collJ021tActaAies = array();
			} else {

				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ008tDivision($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ021tActaAieCriteria) || !$this->lastJ021tActaAieCriteria->equals($criteria)) {
				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ008tDivision($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ021tActaAieCriteria = $criteria;

		return $this->collJ021tActaAies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J021tActaAies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ021tActaAiesJoinJ002tUsuarioRelatedByCoElabora($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ021tActaAies === null) {
			if ($this->isNew()) {
				$this->collJ021tActaAies = array();
			} else {

				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ002tUsuarioRelatedByCoElabora($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ021tActaAieCriteria) || !$this->lastJ021tActaAieCriteria->equals($criteria)) {
				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ002tUsuarioRelatedByCoElabora($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ021tActaAieCriteria = $criteria;

		return $this->collJ021tActaAies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J021tActaAies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ021tActaAiesJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ021tActaAies === null) {
			if ($this->isNew()) {
				$this->collJ021tActaAies = array();
			} else {

				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ021tActaAieCriteria) || !$this->lastJ021tActaAieCriteria->equals($criteria)) {
				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ021tActaAieCriteria = $criteria;

		return $this->collJ021tActaAies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J021tActaAies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ021tActaAiesJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ021tActaAies === null) {
			if ($this->isNew()) {
				$this->collJ021tActaAies = array();
			} else {

				$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J021tActaAiePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ021tActaAieCriteria) || !$this->lastJ021tActaAieCriteria->equals($criteria)) {
				$this->collJ021tActaAies = J021tActaAiePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ021tActaAieCriteria = $criteria;

		return $this->collJ021tActaAies;
	}

	/**
	 * Clears out the collJ022tActaAnies collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ022tActaAnies()
	 */
	public function clearJ022tActaAnies()
	{
		$this->collJ022tActaAnies = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ022tActaAnies collection (array).
	 *
	 * By default this just sets the collJ022tActaAnies collection to an empty array (like clearcollJ022tActaAnies());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ022tActaAnies()
	{
		$this->collJ022tActaAnies = array();
	}

	/**
	 * Gets an array of J022tActaAnie objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J016tForense has previously been saved, it will retrieve
	 * related J022tActaAnies from storage. If this J016tForense is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J022tActaAnie[]
	 * @throws     PropelException
	 */
	public function getJ022tActaAnies($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
			   $this->collJ022tActaAnies = array();
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				J022tActaAniePeer::addSelectColumns($criteria);
				$this->collJ022tActaAnies = J022tActaAniePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				J022tActaAniePeer::addSelectColumns($criteria);
				if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
					$this->collJ022tActaAnies = J022tActaAniePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ022tActaAnieCriteria = $criteria;
		return $this->collJ022tActaAnies;
	}

	/**
	 * Returns the number of related J022tActaAnie objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J022tActaAnie objects.
	 * @throws     PropelException
	 */
	public function countJ022tActaAnies(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				$count = J022tActaAniePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
					$count = J022tActaAniePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ022tActaAnies);
				}
			} else {
				$count = count($this->collJ022tActaAnies);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J022tActaAnie object to this object
	 * through the J022tActaAnie foreign key attribute.
	 *
	 * @param      J022tActaAnie $l J022tActaAnie
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ022tActaAnie(J022tActaAnie $l)
	{
		if ($this->collJ022tActaAnies === null) {
			$this->initJ022tActaAnies();
		}
		if (!in_array($l, $this->collJ022tActaAnies, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ022tActaAnies, $l);
			$l->setJ016tForense($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J022tActaAnies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ022tActaAniesJoinJ007tRegion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
				$this->collJ022tActaAnies = array();
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ007tRegion($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ022tActaAnieCriteria = $criteria;

		return $this->collJ022tActaAnies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J022tActaAnies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ022tActaAniesJoinJ014tNegocio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
				$this->collJ022tActaAnies = array();
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ014tNegocio($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ014tNegocio($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ022tActaAnieCriteria = $criteria;

		return $this->collJ022tActaAnies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J022tActaAnies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ022tActaAniesJoinJ008tDivision($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
				$this->collJ022tActaAnies = array();
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ008tDivision($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ008tDivision($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ022tActaAnieCriteria = $criteria;

		return $this->collJ022tActaAnies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J022tActaAnies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ022tActaAniesJoinJ002tUsuarioRelatedByCoElabora($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
				$this->collJ022tActaAnies = array();
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ002tUsuarioRelatedByCoElabora($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ002tUsuarioRelatedByCoElabora($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ022tActaAnieCriteria = $criteria;

		return $this->collJ022tActaAnies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J022tActaAnies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ022tActaAniesJoinJ002tUsuarioRelatedByCoCustodioNiega($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
				$this->collJ022tActaAnies = array();
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ002tUsuarioRelatedByCoCustodioNiega($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ002tUsuarioRelatedByCoCustodioNiega($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ022tActaAnieCriteria = $criteria;

		return $this->collJ022tActaAnies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J022tActaAnies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ022tActaAniesJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
				$this->collJ022tActaAnies = array();
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioCreated($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ022tActaAnieCriteria = $criteria;

		return $this->collJ022tActaAnies;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J022tActaAnies from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ022tActaAniesJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ022tActaAnies === null) {
			if ($this->isNew()) {
				$this->collJ022tActaAnies = array();
			} else {

				$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J022tActaAniePeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ022tActaAnieCriteria) || !$this->lastJ022tActaAnieCriteria->equals($criteria)) {
				$this->collJ022tActaAnies = J022tActaAniePeer::doSelectJoinJ002tUsuarioRelatedByCoUsuarioUpdate($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ022tActaAnieCriteria = $criteria;

		return $this->collJ022tActaAnies;
	}

	/**
	 * Clears out the collJ031tAccionRecomendadas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ031tAccionRecomendadas()
	 */
	public function clearJ031tAccionRecomendadas()
	{
		$this->collJ031tAccionRecomendadas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ031tAccionRecomendadas collection (array).
	 *
	 * By default this just sets the collJ031tAccionRecomendadas collection to an empty array (like clearcollJ031tAccionRecomendadas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ031tAccionRecomendadas()
	{
		$this->collJ031tAccionRecomendadas = array();
	}

	/**
	 * Gets an array of J031tAccionRecomendada objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J016tForense has previously been saved, it will retrieve
	 * related J031tAccionRecomendadas from storage. If this J016tForense is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J031tAccionRecomendada[]
	 * @throws     PropelException
	 */
	public function getJ031tAccionRecomendadas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ031tAccionRecomendadas === null) {
			if ($this->isNew()) {
			   $this->collJ031tAccionRecomendadas = array();
			} else {

				$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

				J031tAccionRecomendadaPeer::addSelectColumns($criteria);
				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

				J031tAccionRecomendadaPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ031tAccionRecomendadaCriteria) || !$this->lastJ031tAccionRecomendadaCriteria->equals($criteria)) {
					$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ031tAccionRecomendadaCriteria = $criteria;
		return $this->collJ031tAccionRecomendadas;
	}

	/**
	 * Returns the number of related J031tAccionRecomendada objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J031tAccionRecomendada objects.
	 * @throws     PropelException
	 */
	public function countJ031tAccionRecomendadas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ031tAccionRecomendadas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

				$count = J031tAccionRecomendadaPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

				if (!isset($this->lastJ031tAccionRecomendadaCriteria) || !$this->lastJ031tAccionRecomendadaCriteria->equals($criteria)) {
					$count = J031tAccionRecomendadaPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ031tAccionRecomendadas);
				}
			} else {
				$count = count($this->collJ031tAccionRecomendadas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J031tAccionRecomendada object to this object
	 * through the J031tAccionRecomendada foreign key attribute.
	 *
	 * @param      J031tAccionRecomendada $l J031tAccionRecomendada
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ031tAccionRecomendada(J031tAccionRecomendada $l)
	{
		if ($this->collJ031tAccionRecomendadas === null) {
			$this->initJ031tAccionRecomendadas();
		}
		if (!in_array($l, $this->collJ031tAccionRecomendadas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ031tAccionRecomendadas, $l);
			$l->setJ016tForense($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J031tAccionRecomendadas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ031tAccionRecomendadasJoinJ002tUsuarioRelatedByCoResponsableStiEjecutar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ031tAccionRecomendadas === null) {
			if ($this->isNew()) {
				$this->collJ031tAccionRecomendadas = array();
			} else {

				$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelectJoinJ002tUsuarioRelatedByCoResponsableStiEjecutar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ031tAccionRecomendadaCriteria) || !$this->lastJ031tAccionRecomendadaCriteria->equals($criteria)) {
				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelectJoinJ002tUsuarioRelatedByCoResponsableStiEjecutar($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ031tAccionRecomendadaCriteria = $criteria;

		return $this->collJ031tAccionRecomendadas;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J031tAccionRecomendadas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ031tAccionRecomendadasJoinJ002tUsuarioRelatedByCoCreatedAt($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ031tAccionRecomendadas === null) {
			if ($this->isNew()) {
				$this->collJ031tAccionRecomendadas = array();
			} else {

				$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelectJoinJ002tUsuarioRelatedByCoCreatedAt($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ031tAccionRecomendadaCriteria) || !$this->lastJ031tAccionRecomendadaCriteria->equals($criteria)) {
				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelectJoinJ002tUsuarioRelatedByCoCreatedAt($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ031tAccionRecomendadaCriteria = $criteria;

		return $this->collJ031tAccionRecomendadas;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J031tAccionRecomendadas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ031tAccionRecomendadasJoinJ002tUsuarioRelatedByCoUpdateAt($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ031tAccionRecomendadas === null) {
			if ($this->isNew()) {
				$this->collJ031tAccionRecomendadas = array();
			} else {

				$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelectJoinJ002tUsuarioRelatedByCoUpdateAt($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ031tAccionRecomendadaCriteria) || !$this->lastJ031tAccionRecomendadaCriteria->equals($criteria)) {
				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelectJoinJ002tUsuarioRelatedByCoUpdateAt($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ031tAccionRecomendadaCriteria = $criteria;

		return $this->collJ031tAccionRecomendadas;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J031tAccionRecomendadas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ031tAccionRecomendadasJoinJ034tIncidente($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ031tAccionRecomendadas === null) {
			if ($this->isNew()) {
				$this->collJ031tAccionRecomendadas = array();
			} else {

				$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelectJoinJ034tIncidente($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J031tAccionRecomendadaPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ031tAccionRecomendadaCriteria) || !$this->lastJ031tAccionRecomendadaCriteria->equals($criteria)) {
				$this->collJ031tAccionRecomendadas = J031tAccionRecomendadaPeer::doSelectJoinJ034tIncidente($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ031tAccionRecomendadaCriteria = $criteria;

		return $this->collJ031tAccionRecomendadas;
	}

	/**
	 * Clears out the collJ032tSerialesEnlacesIrs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ032tSerialesEnlacesIrs()
	 */
	public function clearJ032tSerialesEnlacesIrs()
	{
		$this->collJ032tSerialesEnlacesIrs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ032tSerialesEnlacesIrs collection (array).
	 *
	 * By default this just sets the collJ032tSerialesEnlacesIrs collection to an empty array (like clearcollJ032tSerialesEnlacesIrs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ032tSerialesEnlacesIrs()
	{
		$this->collJ032tSerialesEnlacesIrs = array();
	}

	/**
	 * Gets an array of J032tSerialesEnlacesIr objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J016tForense has previously been saved, it will retrieve
	 * related J032tSerialesEnlacesIrs from storage. If this J016tForense is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J032tSerialesEnlacesIr[]
	 * @throws     PropelException
	 */
	public function getJ032tSerialesEnlacesIrs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ032tSerialesEnlacesIrs === null) {
			if ($this->isNew()) {
			   $this->collJ032tSerialesEnlacesIrs = array();
			} else {

				$criteria->add(J032tSerialesEnlacesIrPeer::CO_FORENSE, $this->co_forense);

				J032tSerialesEnlacesIrPeer::addSelectColumns($criteria);
				$this->collJ032tSerialesEnlacesIrs = J032tSerialesEnlacesIrPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J032tSerialesEnlacesIrPeer::CO_FORENSE, $this->co_forense);

				J032tSerialesEnlacesIrPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ032tSerialesEnlacesIrCriteria) || !$this->lastJ032tSerialesEnlacesIrCriteria->equals($criteria)) {
					$this->collJ032tSerialesEnlacesIrs = J032tSerialesEnlacesIrPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ032tSerialesEnlacesIrCriteria = $criteria;
		return $this->collJ032tSerialesEnlacesIrs;
	}

	/**
	 * Returns the number of related J032tSerialesEnlacesIr objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J032tSerialesEnlacesIr objects.
	 * @throws     PropelException
	 */
	public function countJ032tSerialesEnlacesIrs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ032tSerialesEnlacesIrs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J032tSerialesEnlacesIrPeer::CO_FORENSE, $this->co_forense);

				$count = J032tSerialesEnlacesIrPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J032tSerialesEnlacesIrPeer::CO_FORENSE, $this->co_forense);

				if (!isset($this->lastJ032tSerialesEnlacesIrCriteria) || !$this->lastJ032tSerialesEnlacesIrCriteria->equals($criteria)) {
					$count = J032tSerialesEnlacesIrPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ032tSerialesEnlacesIrs);
				}
			} else {
				$count = count($this->collJ032tSerialesEnlacesIrs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J032tSerialesEnlacesIr object to this object
	 * through the J032tSerialesEnlacesIr foreign key attribute.
	 *
	 * @param      J032tSerialesEnlacesIr $l J032tSerialesEnlacesIr
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ032tSerialesEnlacesIr(J032tSerialesEnlacesIr $l)
	{
		if ($this->collJ032tSerialesEnlacesIrs === null) {
			$this->initJ032tSerialesEnlacesIrs();
		}
		if (!in_array($l, $this->collJ032tSerialesEnlacesIrs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ032tSerialesEnlacesIrs, $l);
			$l->setJ016tForense($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J032tSerialesEnlacesIrs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ032tSerialesEnlacesIrsJoinJ030tInformeRecomendaciones($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ032tSerialesEnlacesIrs === null) {
			if ($this->isNew()) {
				$this->collJ032tSerialesEnlacesIrs = array();
			} else {

				$criteria->add(J032tSerialesEnlacesIrPeer::CO_FORENSE, $this->co_forense);

				$this->collJ032tSerialesEnlacesIrs = J032tSerialesEnlacesIrPeer::doSelectJoinJ030tInformeRecomendaciones($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J032tSerialesEnlacesIrPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ032tSerialesEnlacesIrCriteria) || !$this->lastJ032tSerialesEnlacesIrCriteria->equals($criteria)) {
				$this->collJ032tSerialesEnlacesIrs = J032tSerialesEnlacesIrPeer::doSelectJoinJ030tInformeRecomendaciones($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ032tSerialesEnlacesIrCriteria = $criteria;

		return $this->collJ032tSerialesEnlacesIrs;
	}

	/**
	 * Clears out the collJ045tReferenciaMarcos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJ045tReferenciaMarcos()
	 */
	public function clearJ045tReferenciaMarcos()
	{
		$this->collJ045tReferenciaMarcos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJ045tReferenciaMarcos collection (array).
	 *
	 * By default this just sets the collJ045tReferenciaMarcos collection to an empty array (like clearcollJ045tReferenciaMarcos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJ045tReferenciaMarcos()
	{
		$this->collJ045tReferenciaMarcos = array();
	}

	/**
	 * Gets an array of J045tReferenciaMarco objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this J016tForense has previously been saved, it will retrieve
	 * related J045tReferenciaMarcos from storage. If this J016tForense is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array J045tReferenciaMarco[]
	 * @throws     PropelException
	 */
	public function getJ045tReferenciaMarcos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
			   $this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

				J045tReferenciaMarcoPeer::addSelectColumns($criteria);
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

				J045tReferenciaMarcoPeer::addSelectColumns($criteria);
				if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
					$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;
		return $this->collJ045tReferenciaMarcos;
	}

	/**
	 * Returns the number of related J045tReferenciaMarco objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related J045tReferenciaMarco objects.
	 * @throws     PropelException
	 */
	public function countJ045tReferenciaMarcos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

				$count = J045tReferenciaMarcoPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

				if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
					$count = J045tReferenciaMarcoPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collJ045tReferenciaMarcos);
				}
			} else {
				$count = count($this->collJ045tReferenciaMarcos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a J045tReferenciaMarco object to this object
	 * through the J045tReferenciaMarco foreign key attribute.
	 *
	 * @param      J045tReferenciaMarco $l J045tReferenciaMarco
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJ045tReferenciaMarco(J045tReferenciaMarco $l)
	{
		if ($this->collJ045tReferenciaMarcos === null) {
			$this->initJ045tReferenciaMarcos();
		}
		if (!in_array($l, $this->collJ045tReferenciaMarcos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJ045tReferenciaMarcos, $l);
			$l->setJ016tForense($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J045tReferenciaMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ045tReferenciaMarcosJoinJ046tMarcoNormativo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ046tMarcoNormativo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ046tMarcoNormativo($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;

		return $this->collJ045tReferenciaMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J045tReferenciaMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ045tReferenciaMarcosJoinJ034tIncidente($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ034tIncidente($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ034tIncidente($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;

		return $this->collJ045tReferenciaMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J045tReferenciaMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ045tReferenciaMarcosJoinJ030tInformeRecomendaciones($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ030tInformeRecomendaciones($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ030tInformeRecomendaciones($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;

		return $this->collJ045tReferenciaMarcos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this J016tForense is new, it will return
	 * an empty collection; or if this J016tForense has previously
	 * been saved, it will retrieve related J045tReferenciaMarcos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in J016tForense.
	 */
	public function getJ045tReferenciaMarcosJoinJ031tAccionRecomendada($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(J016tForensePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJ045tReferenciaMarcos === null) {
			if ($this->isNew()) {
				$this->collJ045tReferenciaMarcos = array();
			} else {

				$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ031tAccionRecomendada($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(J045tReferenciaMarcoPeer::CO_FORENSE, $this->co_forense);

			if (!isset($this->lastJ045tReferenciaMarcoCriteria) || !$this->lastJ045tReferenciaMarcoCriteria->equals($criteria)) {
				$this->collJ045tReferenciaMarcos = J045tReferenciaMarcoPeer::doSelectJoinJ031tAccionRecomendada($criteria, $con, $join_behavior);
			}
		}
		$this->lastJ045tReferenciaMarcoCriteria = $criteria;

		return $this->collJ045tReferenciaMarcos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collJ018tInformeForenses) {
				foreach ((array) $this->collJ018tInformeForenses as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ019tParticipantess) {
				foreach ((array) $this->collJ019tParticipantess as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ020tActaAccs) {
				foreach ((array) $this->collJ020tActaAccs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ021tActaAies) {
				foreach ((array) $this->collJ021tActaAies as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ022tActaAnies) {
				foreach ((array) $this->collJ022tActaAnies as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ031tAccionRecomendadas) {
				foreach ((array) $this->collJ031tAccionRecomendadas as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ032tSerialesEnlacesIrs) {
				foreach ((array) $this->collJ032tSerialesEnlacesIrs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collJ045tReferenciaMarcos) {
				foreach ((array) $this->collJ045tReferenciaMarcos as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collJ018tInformeForenses = null;
		$this->collJ019tParticipantess = null;
		$this->collJ020tActaAccs = null;
		$this->collJ021tActaAies = null;
		$this->collJ022tActaAnies = null;
		$this->collJ031tAccionRecomendadas = null;
		$this->collJ032tSerialesEnlacesIrs = null;
		$this->collJ045tReferenciaMarcos = null;
			$this->aJ002tUsuarioRelatedByCoUsuarioApertura = null;
			$this->aJ002tUsuarioRelatedByCoUsuarioCierre = null;
			$this->aJ007tRegion = null;
			$this->aJ014tNegocio = null;
			$this->aJ008tDivision = null;
			$this->aJ017tAlcanceForense = null;
			$this->aJ002tUsuarioRelatedByCoUsuarioUpdate = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseJ016tForense:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseJ016tForense::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseJ016tForense
